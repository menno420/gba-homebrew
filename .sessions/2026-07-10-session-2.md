# 2026-07-10 — session 2: Lumen Drift increment 1 (Track B)

> **Status:** `complete`

- **📊 Model:** Claude (ID withheld) · high · Lumen Drift increment 1 (first-playable slice) + generic engine scaffolding
- **📊 Time:** started Fri Jul 10 02:49:52 UTC 2026 · closed Fri Jul 10 03:09:30 UTC 2026 (`date -u`)

## Scope

Standing-default work (no unexecuted inbox order beyond 001, which is done for
this repo's half). Coordinator-directed slice: build **Lumen Drift increment
1** — the shortest first-playable cut per
[`../docs/concepts/session-1-concepts.md`](../docs/concepts/session-1-concepts.md).

⚑ context: the owner has NOT ratified the concept pick (needs-owner flag
carried). Mitigation, per directive: small slice; everything reusable factored
into `games/common/` so it transfers to Clockwork Courier or Shoal.

## Shipped

- **PR #5** (heartbeat, merge `a26454b`): born-red card, claim, status WIP —
  plus the second live-fire substrate-gate fix: a session card **ADDED** by a
  PR (born-red heartbeat, conventions rule 10) now gates advisory via the
  absent sentinel; a **MODIFIED** card (close-out flip) keeps the locked door.
  PR #2 had merged with the gate red for exactly this; heartbeats now merge
  green. Staged `.substrate/ci/` copy synced. ⚑ upstream candidate for
  substrate-kit alongside the PR #3 fix.
- **PR #6** (feature, merge `72f5b04`): **Lumen Drift increment 1** — boots
  straight into gameplay: light mote falls through a dark deterministic cave,
  hold A to thrust against gravity, D-pad steers, START restarts, depth
  high-water score on the HUD, light decays with run time and thrust flares
  it back (bg palette fade; sprites stay bright).
  - Generic (`games/common/`): `butano-game.mak` build glue;
    `gl_input.h` (D-pad force vector); `gl_kinematics.h` (fixed-point body);
    `gl_tile_grid.h` (predicate-based axis-separated AABB tile collision).
  - Generic (`tools/headless-screenshot.py`): scripted button injection
    (`--keys START-END:NAME`), multi-shot (`--shot FRAME:PATH`),
    `--require-distinct` interactivity assertion.
  - Game-specific (`games/lumen-drift/`): cave waveform, tuning, light-decay
    trick, HUD, original generated assets (`graphics/generate_assets.py`,
    deterministic stdlib-only; committed BMPs are its byte-exact output).
  - CI "ROM builds" now builds ALL `games/*/Makefile` ROMs (asserts ≥ 2);
    40s warm on PR #6, well under the 60s budget. Local clean build 13.3s.
  - Proof: `docs/proof/session-2-lumen-drift-boot.png` (frame 238, no input,
    DEPTH 3) vs `docs/proof/session-2-lumen-drift-input.png` (frame 899 after
    scripted RIGHT+DOWN, DEPTH 9, camera scrolled) — 37,355 px differ,
    non-blank PASS both.

## Guard recipe (for the next session)

Butano re-invokes make from `build/`, so any game-Makefile include of a
shared fragment MUST resolve it absolute first: see `GAMESCOMMONABS` export
in `games/lumen-drift/Makefile` (same trick as `LIBBUTANOABS`). Symptom
otherwise: `../common/butano-game.mak: No such file or directory` on the
SECOND make pass only.

## 💡 Session idea

Depth-regression harness assertion: extend `tools/headless-screenshot.py`
(or a sibling `tools/assert-state.py`) to OCR-free-check the DEPTH HUD by
sampling the known text-pixel region, so CI can assert "recorded input
sequence reaches depth N" — the concept doc's own regression-test design for
Lumen Drift increment 2 (shards, death/restart, 3 cave sections).

## ⟲ Previous-session review

Reviewed session 1's PRs #2-#4 at HEAD before building: toolchain recipe and
skeleton verified live (setup idempotent-skips in 0.01s; skeleton rebuilds in
1.1s warm). One correction to the session-1 record: PR #2 did NOT merge on
green — its substrate-gate run concluded `failure` (born-red card in diff);
the handoff's "both PRs self-merged on green" was inaccurate. Fixed
structurally this session (PR #5 gate rule) rather than re-litigated.
