# 2026-07-10 — session 4: Lumen Drift increment 3 (Track B)

> **Status:** `in-progress`

- **📊 Model:** Claude (ID withheld) · high · Lumen Drift increment 3 (shards + cave sections) + quick-wins
- **📊 Time:** started Fri Jul 10 03:39:41 UTC 2026 (`date -u`)

## Scope

Standing-default work (inbox re-read at HEAD `28b7a30` — no order beyond 001,
which is done for this repo's half). Coordinator-directed slice: **Lumen Drift
increment 3 — spark shards + cave sections**, completing the concept doc's
committed scope (the two items the "Measured" note lists as not yet built from
the original S2 list). Planned: collectible light shards that reward risk
(score/light interplay per the concept: "collect spark shards that extend your
light radius before it decays"), distinct cave sections as depth increases
(visual/layout variation), deterministic layouts for reproducible headless
proof. Shareable logic (collectible/pickup pattern, section progression) into
`games/common/` where it genuinely generalizes.

Then quick-wins with remaining capacity: HUD-depth CI assertion
(`docs/ideas/hud-depth-ci-assert-2026-07-10.md` — workflow_dispatch/nightly
tier, NOT per-PR, respecting the <60s "ROM builds" budget) and toolchain
checksum-pins (`docs/ideas/toolchain-checksum-pin-2026-07-10.md`).

⚑ context: the owner concept pick is still pending (needs-owner flag carried).
Mitigation, same as sessions 2–3: small slice; anything that genuinely
generalizes (pickup/section patterns) goes to `games/common/`; this session
brings Lumen Drift to the concept doc's full committed scope, so a pick
elsewhere loses nothing further.
