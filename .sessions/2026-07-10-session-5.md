# 2026-07-10 — session 5: upstream gate fixes + Lumen Drift polish (Track B)

> **Status:** `in-progress`

- **📊 Model:** Claude (ID withheld) · high · upstream substrate-gate fixes to substrate-kit + Lumen Drift polish
- **📊 Time:** started Fri Jul 10 04:54:32 UTC 2026 (`date -u`)

## Scope

Standing-default work (inbox re-read at HEAD `8efd9bc` — no order beyond 001,
which is done for this repo's half). Coordinator-directed slice, two phases:

**Phase A — upstream the two substrate-gate fixes to `menno420/substrate-kit`**
(the ⚑ carried in status since session 2):

1. **No-card sentinel** (this repo's commit `799dd66`, landed via PR #3): a PR
   with no session card in its diff passes an explicitly named, nonexistent
   `--session-log` sentinel (advisory per the engine contract) instead of the
   bare mtime fallback, which latched onto the mid-session in-progress card
   and redded every unrelated PR.
2. **Added-card advisory** (commit `0e3b649`, landed via PR #5): a card ADDED
   by the PR (born-red heartbeat — conventions rule 10 requires the first
   commit to carry an in-progress card) gates advisory via the absent
   sentinel; a card MODIFIED by the PR (close-out flips) keeps the full
   locked-door strict gate.

Both validated live across gba-homebrew PRs #3–#14. Upstreaming follows
substrate-kit's OWN control protocol (read at HEAD before acting); if its
active session owns CI-template changes, back off and hand off instead.

**Phase B — Lumen Drift polish** (post-first-complete candidates from the
concept doc's FIRST-COMPLETE note), scoped by remaining capacity. No game code
is touched in phase A.

⚑ context: the owner concept pick is still pending (needs-owner flag carried).
Phase A is pick-independent (pure CI-template upstreaming); phase B mitigation
same as sessions 2–4: small slices, generic parts to `games/common/`.
