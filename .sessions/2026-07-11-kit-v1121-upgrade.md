# 2026-07-11 — kit upgrade: substrate-kit v1.12.0 → v1.12.1 (distribution wave)

> **Status:** `complete`

- **📊 Model:** fable-5 · distribution worker · kit upgrade v1.12.0 → v1.12.1
- **📊 Time:** started Sat Jul 11 22:24:41 UTC 2026 · closed Sat Jul 11 22:31 UTC 2026 (`date -u`)

## Scope

Kit-upgrade ONLY (distribution wave): replace the vendored `bootstrap.py`
with the pinned v1.12.1 release asset (sha256
`1055ca2cfd32a83e3dab7a978b05fbec2a82932a3375de0b1034f2519c16e4aa`, 704108
bytes, verified), run `python3 bootstrap.py.new upgrade`, verify
`python3 bootstrap.py check --strict` exit 0. No domain work; no
control/inbox.md or control/status.md writes (heartbeat `kit:` bump is
lane-owed).

## What shipped

- **PR #59** — substrate-kit **v1.12.0 → v1.12.1** via the pinned release
  asset (sha256 verified twice: locally after download and self-verified by
  the upgrade engine against `release.json`; staged inputs self-cleaned).
  - `bootstrap.py` v1.12.0 → v1.12.1 (dist header + `KIT_VERSION`);
    `substrate.config.json` `kit_version: 1.12.1`; staged `.substrate/`
    artifacts regenerated.
  - **One new bank:** `.substrate/backup/bootstrap-1.12.0.py`, sha256
    `77c00b811429e1b526ccc7e0dcf597435c11048e16a67edba6050f516ad5e1f8` —
    matches the v1.12.0 release asset exactly. All 8 pre-existing banked
    archives verified byte-identical before/after (hashes compared).
  - **Carve-out scan:** `ran, 0 found` (live gate matched the previous kit
    template byte-for-byte); live `.github/workflows/substrate-gate.yml`
    regenerated kit-owned from template@new. No pre-regen bank needed.
  - `python3 bootstrap.py check --strict` — the only mid-session finding was
    this card's own born-red badge (designed HOLD); green expected at flip.
- **Not taken, deliberately:** `--apply-docs` doc re-renders — a lane
  decision, not wave scope.

## Lane-owed follow-ups (decide-and-flag)

- `control/status.md` heartbeat `kit:` line bump to v1.12.1 on the lane's
  next heartbeat (wave seat never writes the heartbeat).

## ⚑ Flags

- ⚑ Self-initiated: none — coordinator-directed distribution wave work only.
- No non-kit anomalies observed in the tree during the upgrade window.

## 💡 Session idea

The upgrade verb's stdout summarizes banks and carve-outs, but the
before/after byte-identity of *pre-existing* banked archives is verified only
by the wave worker hand-hashing them. Cheap kit addition: `upgrade` prints a
one-line `banks: N pre-existing verified identical, 1 added (<name> <sha8>)`
by hashing `.substrate/backup/bootstrap-*.py` before and after its own write
— turns a manual audit step every wave seat repeats into engine-attested
output.

## ⟲ Previous-session review

Reviewed session 23 (PR #58, hourly wake): honest-idle discipline held — it
reported shepherd-check + grooming without inventing work, which keeps the
wake cadence trustworthy. One improvement it surfaces: the wake card notes
kit state in passing but has no standing "kit pin vs latest release" glance;
a one-line `kit: 1.12.0 (latest 1.12.1?)` check in the wake routine would
have let the lane self-report upgrade readiness before the distribution wave
arrived. If there is nothing deeper to improve, that is because the wake
session was deliberately minimal — appropriate for an idle heartbeat.
