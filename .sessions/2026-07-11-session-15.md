# Session 15 — game-lab Track B

> **Status:** `complete`

- date: 2026-07-11 (branch `claude/gloamline-concept`)
- mission: **NEW ARC — owner-picked original zombie-survival game,
  target NINTENDO DS** (owner correction relayed mid-slice: NDS, not GBA
  — arrived after the heartbeat, before the doc was written, so the doc
  was authored NDS-first; no rework needed). This is the CONCEPT SLICE:
  a short, decision-ready design doc that leans deliberately on Lumen
  Drift's PURE-LOGIC engine pieces (Butano itself is GBA-only and does
  not port). The long-carried ⚑ concept-pick is RESOLVED by owner input
  (zombie-survival original, superseding the deepen-LD / Clockwork
  Courier / Shoal menu).
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

The concept doc should read like `docs/concepts/session-1-concepts.md` did
for the owner: pitch, loop, scope, risk — but with two additions this arc
can afford because the engine exists now: an explicit **reused-engine
inventory** (what transfers from Lumen Drift for free) and an explicit
**walking-skeleton cut line** (what slice 1 ships vs what waits), so the
next session can start building without another decision round-trip.

## Previous-session review

- Session 13 (PR #47, merged `860dae7`): bare hourly wake — shepherd check
  clean (zero open PRs), grooming pass empty, no new orders; added the live
  per-card `📊 Model:` line to status, closing the ORDER 003 template gap.
- **Mid-slice:** wake session 14 (PR #48, merged `e1bde50`, status-only, no
  card — matching the 12/13 wake pattern) landed while this slice was in
  flight and took the session-14 number; this card renumbered itself
  14 → 15 and the branch rebased onto `e1bde50` so the close-out status
  overwrite is based on the latest write, not a stale one.
- `main` at session start: `860dae7` (→ `e1bde50` after the rebase). Inbox
  re-read at HEAD before the final status write: ORDERS 001-004 all
  acked/done per status; no new orders. This slice is coordinator-assigned
  work (not an ORDER id), so it is claimed in `control/claims/` (claim
  file in the heartbeat commit), not on the orders line.
- No open PRs at session start.

## What this session did

1. Heartbeat first (this card, born red) + work claim
   (`control/claims/claude-gloamline-concept.md`, released at close-out).
2. Read `games/lumen-drift/src/main.cpp`, `games/common/include/gl_*.h`,
   `tools/` and `docs/current-state.md`; inventoried the reusable engine
   pieces. Mid-slice owner correction (GBA → **NDS**) arrived before the
   doc was written, so the inventory was framed honestly as **pure-logic
   porting** (Butano is GBA-only): pure spawn/world functions,
   check-cave-style host proofs, telemetry-mailbox concept, replay/proof
   harness re-pointed at headless melonDS/DeSmuME, synthesized
   audio/asset pipelines, dist/CI conventions.
3. Committed **`docs/concepts/gloamline-concept.md`** — working title
   **Gloamline** (collision-checked: no commercial game of that name),
   horde-defense core loop with rationale, verbs (move + shove), Shambler
   zombie v0, night-clock survival pressure, dual-screen use
   (touch optional), explicit walking-skeleton cut vs later slices,
   determinism + host-provability plan, and a cosmetic flag that the
   `gba-homebrew` repo will host an NDS game per owner direction.
4. Close-out: card flipped, `control/status.md` overwritten (new arc
   header: NDS zombie-survival; concept-pick ⚑ resolved; next slice =
   NDS toolchain feasibility), claim released, `python3 bootstrap.py
   check --strict` run (result recorded in status), READY PR with
   auto-merge armed at creation.

## Guard recipe (for the next slice)

Slice 2 (NDS toolchain feasibility) should NOT assume mGBA conventions
port: `tools/headless-screenshot.py` is mGBA-API-specific (`core.load_save`
notes, `--elf` watch resolution). Anchor points to evaluate against melonDS
/ DeSmuME headless: scripted input injection, framebuffer capture, and a
memory-watch equivalent for the telemetry mailbox — those three are what
the whole proof discipline stands on. Done-when per the arc plan: minimal
.nds built (libnds or BlocksDS) and boot-verified with a screenshot, or a
six-field OWNER-ACTION naming the exact missing packages.
