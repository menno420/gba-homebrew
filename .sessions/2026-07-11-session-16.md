# Session 16 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-11 (branch `claude/gloamline-nds-feasibility`)
- mission: **Gloamline arc slice 2 — NDS TOOLCHAIN FEASIBILITY GATE.**
  Prove an NDS toolchain in this container (BlocksDS preferred, devkitPro
  nds-dev via mirror as fallback), build a minimal ORIGINAL dual-screen
  .nds, boot-verify it headlessly (melonDS or DeSmuME / py-desmume), wire
  a reproducible CI build path — or ship the exact missing-package
  OWNER-ACTION report if the walls win. Done-when per the arc plan in
  `control/status.md`: minimal .nds built and boot-verified with a
  screenshot, OR a six-field OWNER-ACTION naming the exact missing
  packages.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

The feasibility gate is only worth its slice if it leaves rails, not just a
verdict: a fail-closed pinned setup script (`tools/setup-nds-toolchain.sh`
mirroring `tools/setup-toolchain.sh`'s sha256-pin discipline), a headless
boot checker (`tools/nds-headless-check.py` mirroring
`headless-screenshot.py`'s non-blank verification shape, but dual-screen),
and a CI job — so slice 3 (walking skeleton) starts from `make` + green CI,
not from re-derivation. Known walls to route around, not re-probe:
devkitPro official infra is Cloudflare-403 (mirror-pin pattern exists);
BlocksDS/Wonderful is unprobed territory — capture verbatim errors if it
walls.

## Previous-session review

- Session 15 (PR #50, merged `4bfcf3a`): the Gloamline concept slice —
  decision-ready doc at `docs/concepts/gloamline-concept.md`, NDS-first
  after the mid-slice owner correction. Its guard recipe for THIS slice:
  don't assume mGBA conventions port; the three anchor points to evaluate
  on a headless NDS emulator are scripted input injection, framebuffer
  capture, and a memory-watch equivalent. That recipe drives this
  session's tool design.
- `main` at session start: `4bfcf3a`. No open PRs, no unreleased claims in
  `control/claims/`. Inbox re-read at branch time: no new orders.
- This slice is coordinator-assigned (not an ORDER id) — claimed via
  `control/claims/claude-gloamline-nds-feasibility.md` (this heartbeat).

## What this session did

(in progress — close-out pending)
