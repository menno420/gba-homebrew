# Session 19 — game-lab Track B

> **Status:** `complete`

- date: 2026-07-11 (branch `claude/brineward-concept`)
- mission: **BRINEWARD arc slice 1 — CONCEPT.** Coordinator-assigned: a
  SECOND original NDS game for this repo, single-player pirate
  naval-action (genre-inspired by browser naval-action MMOs: open-water
  sailing, broadside cannon duels, sea monsters, sell loot in port,
  upgrade hull/cannons/sails — reframed single-player, 100% original IP:
  title, art, audio, code). This slice ships the concept doc ONLY
  (`docs/concepts/brineward-concept.md`, modeled on the Gloamline
  concept's structure) — no game dir, no toolchain work, nothing claims
  to build. Runs in parallel with the live Gloamline slice-4 session
  (18, `claude/gloamline-shove-waves`) — zero shared files beyond
  own-section status touches.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

A concept-only slice next to a live feature slice is the cheapest
possible test of the claims convention's parallel-session promise: two
sessions, two claim files, zero shared build surfaces — if this PR and
slice-4's PR both merge without a single conflict outside
`control/status.md`, the one-file-per-claim + own-sections-only
discipline is doing its job and future multi-arc work can overlap
freely. If status.md DOES conflict, record the exact hunk in the card
so the status-section layout can be split per-arc next slice.

## Previous-session review

- Session 17 (PR #52, merged `bc92ad1`): Gloamline walking skeleton
  SHIPPED — playable dist/gloamline.nds, 4 pinned headless proofs,
  telemetry watches. Its guard recipes are Gloamline-specific (dawn-route
  derivation, pinned-assert re-pinning, keypad_update(0) boot trap) —
  none gate THIS slice (docs-only), but all gate Brineward's future
  build slices; the concept doc points at them.
- Session 18 is LIVE in parallel on `claude/gloamline-shove-waves`
  (claim `control/claims/claude-gloamline-shove-waves.md`, born-red card
  committed): Gloamline slice 4, shove + waves. Session number 19 chosen
  to not collide; this slice touches NO Gloamline files.
- `main` at session start: `bc92ad1`. Inbox re-read at branch time: no
  new orders (001/002 done, 003/004 executed — record in status
  history).
- This slice is coordinator-assigned work, not an ORDER — claimed via
  `control/claims/claude-brineward-concept.md` (this heartbeat).

## What this session did

1. Heartbeat first (this card, born red) + work claim, pushed before
   any content work.
2. **Title picked and collision-checked:** generated 6 coined-compound
   candidates (Brineward, Keelfire, Squallmark, Wrackline, Saltspur,
   Brinevane), web-searched the leading three — no game, brand, or
   studio collides with **Brineward** (nearest hits: an unrelated FFXIV
   free company and one indie RPG's spell name); Keelfire and
   Squallmark also clean, kept as recorded runners-up.
3. **`docs/concepts/brineward-concept.md`** (modeled on the Gloamline
   concept's structure): pitch; the single-player out-and-back reframe
   of the naval-MMO loop (open-water momentum sailing, L/R broadside
   batteries with positional dueling, sea monsters that break the
   broadside geometry, flotsam loot with a finite hold, port
   bank/repair/upgrade across hull/cannons/sails); dual-screen layout
   (top = water, bottom = chart table, touch optional / buttons-first);
   controls table; economy sketch with sink-forfeits-hold-only rule;
   original code-authored art + synthesized-audio direction; the lane's
   determinism/host-mirror/telemetry plan reusing the PROVEN NDS rails
   (BlocksDS 1.21.1 pinned setup, nds-headless-check watches,
   keypad_update(0) trap respected, separate future `nds-brineward-build`
   CI job so Gloamline's gates are never touched); v0 walking-skeleton
   cut (sail + ONE enemy sloop, sink-or-be-sunk, restart) + 7-slice
   roadmap; honest open questions (NDS saves unproven, duel-win route
   proof needs a recorder, AI feel is owner-taste, CI-minutes watch,
   repo-name carry).
4. Status own-section update (timestamp + Brineward paragraph; all
   Gloamline/Lumen-Drift sections byte-untouched), inbox re-read at
   HEAD (main `bc92ad1`, no new orders), card flipped, claim released,
   `python3 bootstrap.py check --strict` green, READY PR, auto-merge
   at creation.

## Guard recipe (for the next Brineward slice)

- Concept promises rails, not builds: slice 2 = `games/brineward-nds/`
  scaffold reusing `tools/setup-nds-toolchain.sh` +
  `tools/nds-headless-check.py` AS-IS; add a separate
  `nds-brineward-build` job in `rom-builds.yml` (copy the
  `nds-rom-build` shape) — do NOT add Brineward steps to Gloamline's
  job or the required GBA "ROM builds" gate.
- The duel-WIN proof will need a recorded route vs the deterministic
  AI: promote a `tools/`-level route recorder (session 17's guard
  recipe asked for the same thing for Gloamline waves — build ONE tool
  both arcs share).
- Parallel-session result for the claims ledger: sessions 18 and 19
  overlapped with zero file conflicts at this session's close
  (status.md own-section discipline held; final merge-order conflict
  risk noted in the PR if #53-era merges race).
