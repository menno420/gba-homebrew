# Session 20 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-11 (branch `claude/brineward-skeleton`)
- mission: **BRINEWARD arc slice 2 — WALKING SKELETON.** Coordinator-
  assigned: the v0 playable cut from
  `docs/concepts/brineward-concept.md` — `games/brineward-nds/` with
  momentum sailing (heading + 3-state sail trim) on the top screen, ONE
  enemy sloop on an intercept-and-circle AI, L/R broadside batteries
  with reload + range falloff on BOTH ships, hull bars, sink-or-be-sunk
  → card → START instant restart; bottom screen = ship status v0.
  Rails per the concept doc: pure sim layer (`bw_sim.c`) mirrored by
  `tools/check-brine.py`, `bw_telemetry[16]` mailbox, pinned headless
  proofs (boot / sail kinematics / duel-loss idle / duel-WIN recorded
  route) in a NEW `nds-brineward-build` CI job — Gloamline's job, its
  proofs, and the required GBA gate untouched. `dist/brineward.nds`
  committed byte-deterministic + `docs/PLAYING-BRINEWARD.md`.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

The duel-WIN proof forces this lane's first CLOSED-LOOP route recorder:
Gloamline's dawn route was derived by a scratch search over open-loop
inputs, but out-dueling a firing AI needs a policy (steer to hold the
enemy abeam, fire on alignment) run against the host mirror, with the
recorded input trace replayed open-loop in the emulator. If the
policy-recorded route survives ±-frame alignment shifts like the dawn
route did, the recorder pattern generalizes and Gloamline's slice-4+
waves can adopt it; if it proves brittle, that brittleness is the
finding — record it in the guard recipe either way.

## Previous-session review

- Session 19 (PR #53, merged `39d9630`): Brineward concept doc — this
  slice builds EXACTLY its "walking-skeleton cut (v0)" section; its
  guard recipe (copy the `nds-rom-build` job shape, never touch
  Gloamline's job, promote a route recorder for the duel-win proof) is
  this session's spec.
- Session 17's NDS rails all apply and are reused AS-IS:
  `tools/setup-nds-toolchain.sh` (BlocksDS 1.21.1 pinned),
  `tools/nds-headless-check.py` (`--elf/--watch/--assert-watch`), the
  `keypad_update(0)` KEYINPUT frame-1 boot trap (PLATFORM-LIMITS) and
  the in-ROM `pad_seen_idle` guard, the dist/ provenance convention.
- Session 18 (`claude/gloamline-shove-waves`) may still be live: zero
  shared files this slice beyond own-section `control/status.md`;
  session number 20 and this branch chosen against its claim.
- `main` at session start: `39d9630`. Inbox re-read at branch time: no
  new orders (001/002 done, 003/004 executed — record in status
  history).
- Coordinator-assigned work, not an ORDER — claimed via
  `control/claims/claude-brineward-skeleton.md` (this heartbeat).

## What this session did

(in progress)
