# 2026-07-11 — session 8: ORDER 000 — toolchain verify + reproducible-build hardening (Track B)

> **Status:** `in-progress`

> **Slice 4 open** (same session 8 worker continuing, 2026-07-11 ~02:20 UTC):
> coordinator-directed — Lumen Drift v1.1 deepening: exactly ONE fun-forward
> gameplay addition. Chosen (decide-and-flag): a DEPTH DIFFICULTY CURVE in
> the endless echoes — the bands are currently difficulty-flat forever; every
> 60-row echo cycle past the first will ramp crystal density (and tighten the
> gallery echoes), plateauing at a final tier, with the echo banners
> announcing the tier. Pure world generation stays pure and
> check-cave-provable (periodicity re-anchored at the plateau row); tier 1
> starts beyond both CI replays' reach so replay expectations stay untouched;
> a new recorded deep route + banner/telemetry asserts prove the feature
> fires. Slices 1–3 (PRs #29–#31) are complete and unchanged below; slice 4's
> close-out lands at the bottom. Badge re-flipped born-red for the slice per
> rule 10 — this MODIFIED card rides the locked-door gate.

> **Slice 3** (same session 8 worker continuing, 2026-07-11 02:00–02:08 UTC):
> coordinator-directed — fix narrowed review-queue row #23 (echo-band crystal
> gates: 6 unavoidable-death junctions in rows 191–1093) NOW, with a host-side
> CI checker so this class of defect stays caught; plus downloadable-ROM
> distribution (`dist/` + play doc) so the owner can just download and play.
> Slices 1 (PR #29) and 2 (PR #30) are complete and unchanged below; slice 3's
> close-out lands at the bottom. Badge re-flipped born-red for the slice per
> rule 10 — this MODIFIED card rides the locked-door gate.

> **Slice 2** (same session 8 worker continuing, 2026-07-11 01:37–01:49 UTC):
> ORDER 002 (self-arm wake routine, P1) + review-queue burn-down. Slice 1
> (ORDER 000, PR #29) is complete and unchanged below; slice 2's close-out is
> at the bottom. The badge rode born-red in-progress for the slice per rule
> 10 — this MODIFIED card takes the locked-door gate, which live-fired the
> row #5 re-check.

- **📊 Model:** Claude (ID withheld) · worker (RETRO GAMES studio seat) · ORDER 000 — toolchain + build reproducibility
- **📊 Time:** started Sat Jul 11 01:19:14 UTC 2026 · closed Sat Jul 11 01:25 UTC 2026 (`date -u`)

## Scope

Coordinator-directed slice (studio directive "ORDER 000", owner refs
Q-0259/Q-0265/Q-0266 — NOT an inbox order; inbox re-read at HEAD `0a7689f`
at start AND at close: ORDER 002 (self-arm wake routine) is present and
unexecuted, OUT OF SCOPE for this worker seat — carried in status for the
lane):

1. Verify the pinned toolchain installs in THIS container and build Lumen
   Drift's `.gba` from source locally — size + sha256 recorded.
2. Headless mGBA smoke-test the ROM boots.
3. CI: extend "ROM builds" to upload the `.gba`s as a workflow artifact and
   log their sha256.
4. `docs/BUILDING.md` + `tools/build.sh`: one-command build recipe.

No gameplay-scope changes (Lumen Drift stays SCOPE-COMPLETE; the owner
concept pick stays ⚑ carried).

## Shipped (PR #29)

- **Toolchain verified in a fresh container:** `tools/setup-toolchain.sh`
  ran clean — every mirror package matched its SHA-256 pin; only host
  prerequisite missing was `zstd` (the script's own apt fallback tripped on
  an unrelated stale PPA in this container's sources; installing `zstd`
  directly unblocked it — script unchanged, not worth a carve-out).
- **Lumen Drift built from source locally:**
  `games/lumen-drift/lumen-drift.gba` — **163,636 bytes**, sha256
  `0eb8750a9de8df0846db8598e5b1f13fcb010280f33d4e2c57257bf5318af2cf`
  (~18s cold, ~2s warm). Skeleton also built (114,464 bytes, sha256
  `bec1feae5d351c1fd9be9f808de233b8cd2e9a1e612ff4055c91be6b131a169f`).
- **Headless boot smoke test PASS:** mgba-sdl (system libmgba 0.10.x) +
  the already-pinned mgba==0.10.2 binding on python 3.11.15;
  `tools/headless-screenshot.py`, 300 frames — title screen renders
  (LUMEN DRIFT / BEST 0 / PRESS START), non-blank check green.
- **CI provenance:** `rom-builds.yml` now logs `sha256sum` for every built
  ROM and uploads `games/*/*.gba` + `rom-sha256.txt` as workflow artifact
  `gba-roms-<sha>` (14-day retention, `if-no-files-found: error`). Check
  semantics and <60s budget untouched.
- **One-command build:** `tools/build.sh` (verified end-to-end this
  session) + `docs/BUILDING.md` (fresh-clone recipe, manual build, desktop
  + headless mGBA run instructions with the `--savefile` caveat).

## ⚑ Flags

- ⚑ Inbox ORDER 002 (self-arm hourly wake routine, P1) is UNEXECUTED — out
  of this worker seat's directed scope; the lane's next session should
  claim it first.

## 💡 Session idea

`rom-builds.yml` uploads artifacts keyed by commit sha, but nothing yet
compares the CI-built sha256 against the one recorded in a PR body/card —
a tiny "provenance diff" step (grep the PR body for a sha256 line and warn
on mismatch) would catch "built something different than reviewed" for
free. Cheap, but only worth it once ROMs are actually handed to the owner
regularly.

---

# Slice 2 (same session 8 worker, continued): ORDER 002 + review-queue burn-down

- **📊 Time (slice 2):** opened Sat Jul 11 01:37 UTC 2026 · closed Sat Jul 11 01:49 UTC 2026 (`date -u`)

## Slice 2 scope

1. Inbox ORDER 002 (P1): self-arm the hourly wake routine.
2. Review-queue burn-down: verify and close as many of the 12 open rows as
   the evidence supports, oldest first; fix anything real found.

## ORDER 002 — executed (mechanism + outcome, verbatim)

Mechanism: the `claude-code-remote` MCP server's `create_trigger` tool,
reachable from this session. First attempt targeted the coordinating
retro-games session (`persistent_session_id:
session_01BqCRbGYGeo97sFxMJHzu1e`) and was refused verbatim:
`create_trigger: binding a trigger to another session is not enabled for
this organization. Omit persistent_session_id to bind to the calling
session, or set create_new_session_on_fire=true.` Second attempt succeeded
using the refusal's own fallback: trigger `trig_0137SkvhXEJvwepX8aVNkcSn`,
name `gba-homebrew hourly wake (ORDER 002)`, cron `0 * * * *`,
`create_new_session_on_fire: true` (a FRESH session in this lane's
environment `env_014P62UXP7cuK1bPPWWzg521` on every fire), prompt exactly as
ordered: `Read control/inbox.md at HEAD and run the standing ritual from
your instructions.` Scheduler-reported first fire: `next_run_at
2026-07-11T02:07:07Z` — after this slice's close, so first-fire
confirmation lands in the NEXT wake's status, not this one.

## Review-queue verdicts (11 rows closed, #23 narrowed)

Every claim below was exercised in THIS container on today's build unless
marked otherwise. Build after fixes: `lumen-drift.gba` 163,380 bytes sha256
`4db9f79130fc7145a5311f88325f90c3d5b3ba26de84e3449b73a306b4729f74`;
`skeleton.gba` 114,464 bytes sha256
`bec1feae5d351c1fd9be9f808de233b8cd2e9a1e612ff4055c91be6b131a169f`.

- **#3 CLOSED** — `setup-toolchain.sh` pins are fail-closed (missing pin →
  FATAL, mismatch → FATAL, crtls + Butano git-SHA-pinned); two mirror
  packages re-downloaded live and re-hashed: both match their pins
  (gba-tools `9f3bb4fe…`, devkitarm-rules `35023b2b…`). Skeleton = one
  original `main.cpp` + Butano's zlib font, build artifacts untracked.
  `rom-builds.yml` builds every `games/*/Makefile`, asserts each ROM
  exists, floor `-ge 2`.
- **#5 CLOSED** — gate rule proven both ways locally: modified in-progress
  card + `--require-session-log` → exit 1 (`…missing: a completed Status
  (badge still says in-progress)`); advisory sentinel → exit 0. This PR
  itself live-fires the MODIFIED-card locked door (this card rode born-red
  in-progress until the close-out flip). Upstreaming to substrate-kit still
  open — carried in status.
- **#6 CLOSED, defect fixed** — originality proven by byte-exact
  regeneration of all 3 BMPs from the stdlib-only generator. Collision
  edge cases: epsilon (1/256 px, right/bottom-exclusive) correct; the
  negative-coordinate path was WRONG — `bn::fixed::division(int)`
  truncates toward zero, so `_cell()` misclassified a (cell_size-1)/4096-px
  sliver below every negative cell boundary (host-proven, 35 mispredictions
  in ±40px; fix: floor-to-int-pixels then floored integer division, zero
  mispredictions, positive coords bit-identical). `--require-distinct`
  100px floor is conservative: real consecutive proof shots differ by
  31k+ px.
- **#8 CLOSED** — PR #8's actual gate log (run 29066483426) says verbatim:
  `card .sessions/2026-07-10-session-3.md is newly ADDED by this PR
  (born-red heartbeat) — advisory sentinel gate` → advisory, as designed.
- **#9 CLOSED** — margins coherent (`hazard_half 2 < mote_half 3 <
  pickup_half 4`); surge kill line `surge_y` always lies INSIDE the drawn
  front row (front row is drawn the moment the line enters it), so every
  surge death shows surge tiles overlapping the mote — no invisible kill;
  `surge_start -32` and 0.25 px/f steps are exact fixed-point multiples
  (no negative-sliver interaction). `gl_run_state.h` clean (idempotent
  fail, high-water restore_best, capped timer). Hazard tile regen
  byte-exact.
- **#12 CLOSED** — the deep's +8 phase offset does line row 44 up under
  row 43 (shared open columns 17–23, host-verified); no impassable
  consecutive-row pair anywhere in rows 5–1200 (solidity); pickup(4) vs
  hazard(2) generosity ordering coherent; `gl_stage.h` + `visit_overlaps`
  reviewed clean; shard/rim tiles regen byte-exact.
- **#13 CLOSED** — two pins independently re-derived from the live mirror
  today (see #3), both match the trust-on-first-use snapshot; fonts/
  palettes unchanged so `--assert-text` ink assumptions stand;
  `headless-boot.yml` (incl. lumen-drift-proof) confirmed
  workflow_dispatch-only.
- **#16 CLOSED** — `gl_save.h` magic-tag scheme sound (fresh 0xFF SRAM ≠
  tag; ≤7-char tag + NUL padding correct); the single-slot torn-write
  window at death REMAINS by design — double-buffer idea stays carried,
  payload is one int so worst case is a wrong best score. +4 replay offset
  re-verified by RUNNING both replay tiers locally today (green);
  `--savefile` bus-copy path exercised: two-boot SRAM proof green
  (BEST 0 → death at DEPTH 3 → separate boot shows BEST 3).
- **#17 CLOSED** — mgba binding pin still 0.10.2 and python still 3.11 in
  both workflow jobs (and this container: py3.11.15 + mgba 0.10.2 working);
  the revisit-condition is documented in the workflow comment where any
  future pin-mover will trip over it.
- **#20 CLOSED, defect fixed** — audio empirics re-confirmed live against
  the same pins: full canonical watch set green including maxmod
  silence/voiced transitions on the 0x420 buffer and the deliberate
  exact-count pins `hook[1] eq 48/49`. `gl_light_window.h` review found a
  real latent OOB: `_half_widths[dy]` was indexed whenever `dy <= radius`,
  but the table holds 161 entries — an off-screen center with radius > 160
  reads past it (unreachable from Lumen Drift's clamped camera; fixed with
  an exact bounds guard). H-Blank per-line clamp (min(…,160)) and
  rebuild-on-radius-change logic correct.
- **#23 NARROWED** — verified: purity + tripwires (both replay tiers
  re-run locally on today's build, all text/watch/depth asserts green),
  `_taken` 24-cap eviction (evict-off-window + failsafe pop), hook slots
  4-6 contract, +4 offset. FOUND: the echo-band passability argument is
  solidity-only and has a real hole — 6 junctions in rows 191–1093 where
  every shared open column is crystal'd in one of the two rows =
  unavoidable death gates in the endless echoes (committed rows 0–63 are
  clean). Left as the narrowed row #23: fixing it rewrites endless-content
  hazards and needs deep-replay revalidation — its own slice, sensibly
  after the owner concept pick.
- **#29 CLOSED, floor aligned** — concur with session 8's rail judgment:
  CI ROM artifacts are repo-scoped, login-gated, 14-day-expiring builds of
  already-public original code — build provenance, not external publishing
  (rule 14 untriggered). `tools/build.sh` floor raised `-ge 1` → `-ge 2`
  to match the workflow. BUILDING.md recipe re-exercised end-to-end this
  slice (build.sh + headless runs above).

## Slice 2 ⚑ / carried

- ⚑ owner concept pick unchanged (Lumen Drift / Clockwork Courier / Shoal).
- Carried: PR #5 gate-rule upstream to substrate-kit; gl_save double-buffer
  idea; echo-band crystal-gate fix (narrowed row #23).

## 💡 Slice 2 idea

The host-side mirror of the pure row functions (continuity + crystal-gate
checker, session scratchpad) is exactly the kind of proof that should live
in-repo: a `tools/check-cave.py` that CI could run in seconds would have
caught the crystal gates at PR #23 time and pins the passability contract
for any future `layout_of_row` edit.

## ⟲ Previous-session review

Reviewed the v1.8.0 kit-upgrade session (PR #28, card
`2026-07-11-kit-upgrade-v1.8.0.md`): clean scope discipline and the
carve-out scan section makes the gate regen auditable. Its "Follow-ups for
the lane" sweep concern was warranted — this session found them still
unclaimed (apply-docs, legacy root `claims/` migration, auto-merge enabler
wiring) and re-carries them in status rather than silently dropping them;
its `kit:` line follow-up IS satisfied by this session's status overwrite
(v1.8.0 · check green).

---

# Slice 3 (same session 8 worker, continued): row #23 fix + download-and-play distribution

- **📊 Time (slice 3):** opened Sat Jul 11 02:00 UTC 2026 · closed Sat Jul 11 02:08 UTC 2026 (`date -u`)

## Slice 3 scope

Coordinator-directed: (1) fix the narrowed review-queue row #23 NOW (echo-band
crystal gates — a playability defect regardless of the pending owner concept
pick), with a host-side CI proof so the defect class stays caught; (2) make
Lumen Drift download-and-play for the owner (`dist/` ROM + play doc); (3)
queue the GitHub Release as an owner-click.

## Row #23 — root cause, fix, proof (CLOSED)

**Root cause:** `row_features` places each 2-cell crystal cluster at one edge
of its row's open span using only that row's own layout. At echo-band
junctions where the band width changes, the span of columns OPEN IN BOTH
consecutive rows can shrink to exactly those 2 cells — every crossable column
carries a crystal, an unavoidable death gate. Slice 2's scan (rows ≤1200)
found 6 (junctions 191/192, 287/288, 317/318, 767/768, 967/968, 1093/1094);
over the full generation the true count was **126 gates across two 10080-row
echo periods** (63 per period, repeating forever).

**Fix (pure, neighbour-aware, minimal):** in `row_features`, each crystal
cell is kept only if at least one crystal-free shared-open column survives
toward BOTH neighbouring rows (`junction_open` guard; neighbour solidity is
itself a pure function of the row, so `row_features` stays pure). On every
already-clean row the guard is a no-op — any subset of a clean placement is
clean — host-proven: rows 0–63 AND the whole replay reach (rows 0–130) are
cell-for-cell identical pre/post fix; the guard only drops one crystal cell
per gate row (and on 9 rows/period the shard then re-hugs the freed edge
cell — all beyond row 2100, far outside any replay's reach).

**Proof, all on today's fixed build in THIS container:**

- **Junction scan 0 gates:** `tools/check-cave.py` (NEW, in-repo, stdlib-only,
  ~0.3s) mirrors the three pure row functions, first SELF-VERIFIES the
  10080-row echo periodicity empirically (row 64+k == row 64+10080+k
  cell-for-cell over a full period), then scans rows 0–20226: **0 solidity
  breaks, 0 crystal gates** — coverage of every row the endless cave will
  ever generate, not just a sampled prefix. Pre-fix mirror on the same scan:
  126 gates. Wired into the per-PR "ROM builds" gate (runs before the build;
  lockstep rule commented in both files).
- **Canonical replay green** (full session-4 script +4: all text asserts incl.
  `DEPTH 45 - BEST 45`, all audio watch asserts incl. `hook[1] eq 48/49`).
- **Deep-run replay green** (DEPTH 58/61/95 numeric telemetry + both
  echo-band banners + alive at frame 2263).
- **SRAM two-boot proof green** (BEST 0 → death DEPTH 3 → separate boot
  BEST 3) and **300-frame boot check green**.
- **Build:** `lumen-drift.gba` **163,572 bytes**, sha256
  `88d8146a066d15ee89533e0af80c216eea040ce2027e810182a54f2a5216b697`
  (skeleton unchanged: 114,464 B `bec1feae…`). Row #23 deleted from
  `docs/review-queue.md` — **the queue is now EMPTY.**

## Download-and-play distribution

- **`dist/lumen-drift.gba` committed** (the exact build above) +
  `dist/README.md` (version/date, size, sha256, source-tree provenance, build
  recipe pointer). Rationale: 100% original content → committing the binary is
  publish-safe by construction; rule 14 only gates EXTERNAL publishing. This
  supersedes the earlier "ROMs stay uncommitted" status note by coordinator
  direction (mission: owner downloads one file and plays). `skeleton.gba`
  stays CI-artifact-only.
- **`docs/PLAYING.md`** (NEW, original writing): download path, mGBA desktop +
  phone setup, controls table, goal, what DEPTH/BEST/SPARKS mean, hazard
  primer. Linked prominently from README ("▶ Play it" + a `dist/` row in
  "Where things live").
- **⚑ GitHub Release queued as owner-click** (worker seat: tags/releases 403)
  — exact steps in `control/status.md`.

## Slice 3 ⚑ / carried

- ⚑ owner concept pick unchanged (coordinator has put the menu to the owner).
- ⚑ NEW owner-click: create the Lumen Drift release (steps in status).
- Carried: PR #5 gate-rule upstream; gl_save double-buffer idea; kit
  follow-ups (apply-docs, legacy root `claims/`, auto-merge enabler wiring);
  merged-branch cleanup (worker 403 on branch-delete).

## 💡 Slice 3 idea

`check-cave.py`'s periodicity self-check pattern (verify the closure bound
empirically, then scan the closed domain) generalizes: the same trick would
let a future `check-audio.py` prove "every echo band announces itself" or
"every band contains ≥1 shard run" over ALL content for free — worth doing if
endless-content tuning ever becomes taste-driven.
