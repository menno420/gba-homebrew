# 2026-07-11 — session 8: ORDER 000 — toolchain verify + reproducible-build hardening (Track B)

> **Status:** `in-progress`

- **📊 Model:** Claude (ID withheld) · worker (RETRO GAMES studio seat) · ORDER 000 — toolchain + build reproducibility
- **📊 Time:** started Sat Jul 11 01:19:14 UTC 2026 (`date -u`)

## Scope

Coordinator-directed slice (studio directive "ORDER 000", owner refs
Q-0259/Q-0265/Q-0266 — NOT an inbox order; inbox re-read at HEAD `0a7689f`
at start: ORDER 002 (self-arm wake routine) is present and unexecuted, but
is OUT OF SCOPE for this worker seat — carried to status for the lane):

1. Verify the pinned toolchain installs in THIS container
   (`tools/setup-toolchain.sh`) and build Lumen Drift's `.gba` from source
   locally — record size + sha256.
2. Headless mGBA smoke-test the ROM boots (the binding is already pinned
   here: mgba==0.10.2 on python 3.11) — honest skip if infeasible.
3. CI: extend the "ROM builds" workflow to upload the built `.gba`s as a
   workflow artifact and log their sha256 (build-provenance; the check
   itself already gates every PR).
4. `docs/BUILDING.md`: one-command build doc + how to run in mGBA.

No gameplay-scope changes (Lumen Drift stays SCOPE-COMPLETE; the owner
concept pick stays ⚑ carried).
