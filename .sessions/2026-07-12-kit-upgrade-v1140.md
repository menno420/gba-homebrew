# Session — kit upgrade v1.13.0 → v1.14.0

> **Status:** `complete`

Distribution-only session: upgraded substrate-kit v1.13.0 → v1.14.0 (PR #71).

📊 Model: fable-5
🕒 Start: 2026-07-12 13:45 UTC
🕒 End: 2026-07-12 13:47 UTC

## What happened

- Staged `bootstrap.py.new` + `release.json` from the v1.14.0 release; three-way sha256 verified (asset == release.json field == kit `origin/main:dist/bootstrap.py` == `47c1b8b9…e581`, 779399 bytes).
- `python3 bootstrap.py.new upgrade` — 1.13.0 banked to `.substrate/backup/bootstrap-1.13.0.py`, pre-existing banks byte-identical, carve-out scan on `substrate-gate.yml` ran with 0 found.
- `python3 bootstrap.py upgrade --apply-docs` — applied 5 consumer-untouched template-improved docs (CONSTITUTION.md, collaboration-model.md, question-router.md, CAPABILITIES.md, SKILLS.md). Capability seed: whole-file refresh, no fence-only refresh needed.
- `python3 bootstrap.py check --strict` green except this card's own designed born-red hold.

## Lane-owed follow-ups (recorded, not done — distribution-only scope)

- `control/README.md` diverged (template + doc both moved) — manual merge owed; template delta preserved in `.substrate/upgrade-report.md`.
- Q-0270 collapse note: if this repo carries a local prose copy of the boot-triad/venue-posture rule, collapse it into a pointer to `docs/CAPABILITIES.md`'s posture rule.

## 💡 Session idea

Distribution upgrades would go faster fleet-wide if the upgrade-report's "Template deltas for diverged docs" section also emitted a ready-to-apply `git apply`-able patch hunk per diverged doc (best-effort three-way), so the lane session doing the manual merge starts from a candidate merge instead of a raw template diff.

## ⟲ Previous-session review

Session 35 (#70) kept the heartbeat loop tidy with a wholesale `control/status.md` overwrite — clean and low-noise. One workflow improvement it surfaces: heartbeat sessions could also glance at `.substrate/state.json`'s kit version vs the latest kit release and flag an upgrade being due, which would have queued this v1.14.0 upgrade a cycle earlier.
