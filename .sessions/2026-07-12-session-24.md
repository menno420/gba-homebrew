# Session 24 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-12 (branch `claude/gloamline-scavenge`)
- mission: **Gloamline arc slice 6 — the between-nights SCAVENGE
  INTERLUDE.** The concept doc's stated next cut and slice 5's own
  flag ("the scavenge interlude NEXT slice becomes the real plank
  source"): at dawn, SELECT enters a timed daylight interlude in the
  same yard — the lamplighter returns to the lamppost, the leftover
  dead fall back to the fence line and keep coming, and plank caches
  lie scattered where the night left them. Walk over a cache to pocket
  a plank (never wasting one on a full pocket); START leaves early;
  when the dawn light runs out the next night begins. Taking the
  interlude REPLACES the flat +2 dawn grant — loot what you carry out.
  START at the dawn card keeps the old skip-straight-to-night path
  bit-identical (slice-5 guard recipe: new state stays inert until the
  new button, old proofs press START only — zero re-pins expected).
  Keep all 112 existing asserts green; extend host proof + mirror +
  route tool in lockstep; new pinned headless proofs for entry/loot/
  early-exit, risk (the interlude can kill you), and timer-expiry
  night start; refresh dist + docs. 100% original content.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

(placeholder — filled at close-out)

## Previous-session review

- Session 21 (PR #56, merged `860dae7`-era main; now under `d1ec24f`):
  slice 5 BARRICADES shipped — 9 pinned proofs / 112 asserts, no-trap
  invariants by construction, GL_STRESS ring re-measure (steady 68/71
  scanlines — the arc's thinnest rail), dist 110,080 B. Its guard
  recipe gates THIS slice and is being followed: (1) the interlude is
  a NEW STATE entered only via a NEW button (SELECT) on the dawn card
  — START keeps the old path, old proofs never press SELECT, so all
  112 pins should hold unchanged; (2) three-way LOCKSTEP (`gl_sim.c` ↔
  `tools/check-gloam.py` ↔ `tools/gloam-route.py`) — every new pure
  function and the new state land in all three in the same commit;
  (3) scavenge UI is new-state per-frame work (allowed), but the
  PLAYING-state frame cost is re-checked against the 71-line budget
  anyway (5 extra hidden-OAM writes + 8 telemetry words ride every
  frame); (4) py-desmume BGR screenshot swap respected (no pixel-color
  asserts); (5) keypad_update(0) boot trap + pad_seen_idle guard +
  ±6-frame skew envelope respected by every new route.
- `main` at session start: `d1ec24f` (PR #59, kit v1.12.1). Sessions
  22/23 were bare hourly wakes (heartbeat-only) that both left the
  scavenge interlude for a directed session — this is that session,
  coordinator-assigned.
- Session numbering: a sibling worker is concurrently landing a
  Brineward slice as session 25; this card claims 24.
- Coordinator constraint for this seat: `control/**` is
  coordinator-only this session — no claim file and no status.md
  overwrite from this worker (deviation from the standing ritual,
  flagged here so the lane's next wake can reconcile status.md).

## What this session did

(in progress)
