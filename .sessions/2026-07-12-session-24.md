# Session 24 — game-lab Track B

> **Status:** `complete`

- date: 2026-07-12 (branch `claude/gloamline-scavenge-interlude`)
- mission: **Gloamline arc slice 6 — the between-nights SCAVENGE
  INTERLUDE.** The concept doc's stated next cut and slice 5's own
  flag ("the scavenge interlude NEXT slice becomes the real plank
  source"): at dawn, SELECT enters a timed daylight interlude in the
  same yard — the lamplighter returns to the lamppost, the leftover
  dead fall back to the fence line and keep coming, and plank caches
  lie scattered where the night left them. Walk over a cache to pocket
  a plank (never wasting one on a full pocket); START leaves early;
  when the dawn light runs out the next night begins. Taking the
  interlude REPLACES the flat +2 dawn grant — loot what you carry out.
  START at the dawn card keeps the old skip-straight-to-night path
  bit-identical (slice-5 guard recipe: new state stays inert until the
  new button, old proofs press START only — zero re-pins expected).
  Keep all 112 existing asserts green; extend host proof + mirror +
  route tool in lockstep; new pinned headless proofs for entry/loot/
  early-exit, risk (the interlude can kill you), and timer-expiry
  night start; refresh dist + docs. 100% original content.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

Teleport-to-a-proven-set is free safety: the interlude's "no instant
death" guarantee cost zero new proof code because entry REUSES two
already-proven pure functions as its only geometry — the player goes
to GL_PLAYER_START and every Shambler goes to gl_spawn_of_night(...),
and the safe-radius proof over that exact pair has been in CI since
slice 3. When a new phase needs a safe initial configuration, don't
invent one and prove it — *place the phase on a configuration the
proofs already cover*. (Same move available for lantern-oil: define
the light circle around GL_PLAYER_START and the spawn safe radius
already bounds first-frame darkness contact.)

## Previous-session review

- Session 21 (PR #56, merged; main at `d1ec24f` this session): slice 5
  BARRICADES shipped — 9 pinned proofs / 112 asserts, no-trap
  invariants by construction, GL_STRESS ring re-measure (steady 68/71
  scanlines — the arc's thinnest rail), dist 110,080 B. Its guard
  recipe gated THIS slice and was followed to the letter: (1) the
  interlude is a NEW STATE entered only via a NEW button (SELECT) on
  the dawn card — START keeps the old path, old proofs never press
  SELECT, and **all 112 pre-slice-6 asserts re-ran UNCHANGED against
  the new ROM in-container (zero re-pins — the trick held a FOURTH
  time)**; (2) three-way LOCKSTEP (`gl_sim.c` ↔ `tools/check-gloam.py`
  ↔ `tools/gloam-route.py`) — the new pure functions and the SCAVENGE
  state landed in all three in the same commit, and the slice-3/4
  routes reproduce on the updated mirror with zero drift; (3) the
  PLAYING-state frame cost was re-measured (5 hidden-OAM writes + 8
  telemetry words ride every frame): steady max 69 vs 68 — ~1 modeled
  line, still under the 71 budget; (4) no pixel-color asserts
  (py-desmume BGR swap respected); (5) keypad_update(0) boot trap +
  pad_seen_idle guard (SELECT is gated on it too) + ±6-frame skew
  envelope respected by every new route.
- Sessions 22/23 were bare hourly wakes (heartbeat-only) that both
  deferred this slice to a directed session — this was that session,
  coordinator-assigned.
- Session numbering: sibling worker concurrently landing Brineward
  slice 3 as session 25 (their born-red card `5e38370` seen).
- Coordinator constraint for this seat: `control/**` was
  coordinator-only this session — no claim file and no status.md
  overwrite from this worker (deviation from the standing ritual,
  flagged for the lane's next wake to reconcile status.md).

## What this session did

**SLICE 6 SHIPPED.** All proofs below ran green in this container this
session; every pinned emulator value was mirror-predicted first and
matched exactly.

1. Heartbeat first (this card, born red).
2. **Pure layer** (`gl_sim.h/.c`, mirrored same commit):
   `gl_cache_of_interlude` (pure f(seed, night, index) on a salted
   hash stream, interior box inset 16 px — never on the spawn
   perimeter), `gl_cache_grab` (12 px Chebyshev reach),
   `gl_planks_after_grab` (+1, capped 9). Constants:
   GL_SCAVENGE_FRAMES 1200, GL_CACHE_COUNT 5 — all one-constant
   owner-tunables.
3. **main.c**: STATE_SCAVENGE (4). SELECT on the dawn card (gated on
   pad_seen_idle) recenters the player on the lamppost, returns every
   Shambler to its own night spawn point, scatters the caches, arms
   the 20 s clock; grabs never fire on a full pocket (no waste);
   START leaves early; timer expiry starts night N+1 by itself; the
   scavenge path deliberately SKIPS gl_planks_at_dawn (loot replaces
   the grant — decide-and-flag, concept doc gives no economy spec).
   The PLAYING verbs were extracted verbatim into shared helpers
   (do_shove_verb / do_barricade_verb / step_the_dead /
   the_cold_hands) so the interlude runs the exact same rules —
   no-drift evidence = the 112-assert re-run. Fourth code-authored
   sprite (plank cache, barricade palette), watch-map `*` marks +
   legend, SCAVENGE HUD line, telemetry 32 → 40 (slots 0-31
   untouched: scav clock, caches up, scavenged, interludes, nearest
   cache x/y/dist).
4. **Host proof** (`tools/check-gloam.py`, 1.6 s): +2 proofs — cache
   schedule pure/interior/off-fence (16,640 positions); interlude
   honesty (1,664 greedy lamppost routes ALL fit the dawn light —
   worst 431 vs 1200 frames — under the conservative
   worst-optimal-speed bound, and 5 caches > the 2-plank grant they
   replace); plank-grab economy folded into the plank proof. Entry
   safety needed NO new proof: it reuses gl_spawn_of_night +
   GL_PLAYER_START, whose safe-radius proof has gated CI since
   slice 3 (see 💡).
5. **Mirror + routes** (`tools/gloam-route.py`): GloamSim carries the
   full interlude (same helper split as main.c); slice-3/4 committed
   routes reproduce exactly — zero drift. Three new routes derived on
   the mirror, each surviving every ±6-frame movement skew:
   `proof/scavenge-loot-keys.txt` (grab 3 → pocket caps at 9, stand
   on a 4th untouched, START out), `proof/scavenge-idle-keys.txt`
   (idle → the returned Shambler kills you inside the interlude),
   `proof/scavenge-kite-keys.txt` (autopilot kite survives the full
   1200 frames → night 2 starts itself).
6. **CI: 12 proofs / 177 asserts** (112 carried unchanged + 65 new):
   proof 10 loot/no-waste/early-exit (planks 9 at night 2 vs the skip
   grant's 8 — the economy fork is emulator-visible); proof 11 risk
   (recenter pinned exactly; teleport dist 17366 = 67.8 px, closes
   6821 → 2789 → death at contact 2405 — proof 3's own number);
   proof 12 timer (1190 → 660 → 5 → night 2 auto-start, loot
   carried, player recentered). Screenshots:
   `proof/slice6-scavenge-{loot,death,kite}.png`.
7. **Ship**: `dist/gloamline.nds` 112,128 B, sha256 `ce0bfd25…`,
   byte-deterministic (two clean builds identical + an independent
   fresh-worktree build reproduced the same hash).
   PLAYING-GLOAMLINE scavenge guide; BUILDING, current-state,
   dist/README refreshed. `python3 bootstrap.py check --strict` green
   at close (only the designed born-red hold before this flip).

## Anomaly log (for the coordinator + next wake)

- **Shared-container sibling collision:** the Brineward session-25
  worker shares this container's original checkout AND branch — their
  born-red commit `5e38370` became the parent of this session's first
  card commit, and both slices' uncommitted edits interleaved in one
  working tree (incl. shared files `rom-builds.yml`, `dist/README.md`).
  Resolution: this slice re-based itself into a clean `git worktree`
  cut from `origin/main` (`claude/gloamline-scavenge-interlude`),
  carrying ONLY Gloamline files; the accidental sed touch to the
  shared workflow file in the sibling's tree was reverted; zero
  Brineward files touched here. Expect a textual merge in
  `rom-builds.yml` (disjoint job sections) and the usual
  `dist/README.md` both-rows resolution when the second PR lands.

## Guard recipe (for the next slice — lantern-oil light pressure)

- **Frame budget is the thinnest rail: steady worst 69 of 71 modeled
  scanlines** (mean 57.2, spike 141 < 263) on the GL_STRESS build.
  Slice 6's 5 hidden-OAM writes + 8 telemetry words cost ~1 line over
  slice 5's 68. Lantern-oil light effects are PLAYING-state per-frame
  work — re-measure with `--watch-log` on GL_T_VLINES BEFORE building
  far; budget-shrinking ideas if it overflows: alternate-frame
  watch-map redraw, or coarser HUD refresh.
- **Zero-re-pin shape for oil:** keep the oil gauge inert until a NEW
  input/threshold old proofs never cross; if oil DRAINS passively it
  changes every night-1 frame — then bit-equivalence is impossible and
  a deliberate full re-pin pass is the honest cost (proofs 2-6, 8-11
  all pin night-1 values; the mirror predicts every new value, so a
  re-pin pass is mechanical).
- **Interlude numbers are decide-and-flag owner-tunables**
  (`gl_sim.h`): 1200-frame light, 5 caches × 1 plank, 12 px reach,
  16 px inset, loot-replaces-grant. Owner feel-check invited.
- **SELECT is in the ARM7 exit combo** (SELECT+START+L+R): never
  script SELECT together with START+L+R in a headless run or the
  default ARM7 core powers the ROM off.
- The `keypad_update(0)` boot trap, pad_seen_idle guard (now also
  gating SELECT), ±6-frame skew envelope, GL_STRESS `make clean`
  discipline, and the py-desmume BGR screenshot swap all still apply.
