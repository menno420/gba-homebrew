# Session 25 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-12 (branch `claude/brineward-loot-gold`)
- mission: **BRINEWARD arc slice 3 — LOOT + GOLD.** Coordinator-assigned:
  roadmap item 1 from `docs/concepts/brineward-concept.md` (loot/gold →
  port+upgrades → the Maw → …): a sunk rum-runner drops flotsam crates
  the player scoops by sailing through; finite hold (cap 8); the Graywake
  pier banks the hold (banked gold is safe forever); sinking forfeits the
  unbanked hold; visible gold counter (HUD + ledger). Rails carried from
  slice 2: pure sim `bw_sim.{h,c}` mirrored by `tools/check-brine.py`
  (lockstep rule), telemetry mailbox EXTENDED 16→20 (words 0-15 pinned,
  per the session-20 guard recipe — never re-map), pinned headless proofs
  in the `nds-brineward-build` CI job (Gloamline's job + the GBA gate
  untouched), `dist/brineward.nds` refresh, byte-deterministic build.
- session number 25 claimed (a sibling is concurrently landing Gloamline
  slice 6 as session 24).
- **📊 Model:** fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## Previous-session review

- Session 20 (PR #55, merged `75ef9ba`): the walking skeleton this slice
  builds on. Its guard recipe is this session's spec: sim-constant
  changes invalidate the recorded route + pinned asserts (rerun
  `record-duel-win.py`, re-pin from a local headless run); assert the
  latched seed FIRST in every proof; judge palettes on a real emulator
  (proof PNGs are R/B-swapped); affine sprites have no hide bit; and the
  loot slice should EXTEND the mailbox 16→N, never re-map words 0-15.
- Plan keeps `bw_duel_step`'s duel dynamics bit-identical (loot only
  exists after the sink frame), so proofs 1-3 and the committed duel-win
  route should carry with ZERO re-pins; the win proof gains loot asserts.
