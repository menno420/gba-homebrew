# Session 27 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-12 (branch `claude/gloamline-lantern-oil`)
- mission: **Gloamline arc slice 7 — LANTERN-OIL LIGHT PRESSURE.** The
  concept doc's stated next cut ("lantern oil — light radius shrinks
  as it burns, dark edges spawn faster; the Lumen-Drift-style
  decay-you-can-buy-back pressure") and session 24's flagged next arc
  item: the lantern burns oil every night frame; while the tank is
  healthy the light holds full, but once it runs low the light radius
  shrinks and the dead in the dark stop hesitating (the stagger skip
  that slows every Shambler is cancelled beyond the lamplight — the
  gloam presses). Oil is bought back ONLY in the scavenge interlude:
  oil flasks now lie among the plank caches (pure f(seed, night,
  index) on their own salted stream), never wasted on a full tank.
  The START skip path grants no oil — slice 6's loot-replaces-grant
  shape, reused. All tunables one-constant in `gl_sim.h`. Zero-re-pin
  shape per session 24's guard recipe: the full tank outlasts every
  window an old proof crosses (drain is passive but its EFFECT is
  gated on an oil threshold no pre-slice-7 route reaches), so all 177
  existing asserts should re-run unchanged; three-way lockstep
  (`gl_sim.c` ↔ `tools/check-gloam.py` ↔ `tools/gloam-route.py`); new
  pinned headless proofs for the burn/gauge, the dark press at low
  oil (deep-run route), and the flask buy-back; GL_STRESS re-measure
  with the low-oil path LIVE on the measured frames; refresh dist +
  docs. 100% original content.
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

(placeholder — filled at close-out)

## Previous-session review

- Session 24 (PR #62, merged; main at `8228ef9` this session): slice 6
  SCAVENGE INTERLUDE shipped — 12 pinned proofs / 177 asserts, zero
  re-pins (the trick's fourth hold), dist 112,128 B sha256
  `ce0bfd25…`. Its guard recipe gates THIS slice and is being
  followed: (1) frame budget is the thinnest rail (steady worst 69 of
  71 modeled scanlines) — lantern-oil is PLAYING-state per-frame work,
  re-measure GL_STRESS early, with the low-oil render/step path live;
  (2) zero-re-pin shape for oil — keep the pressure inert until a
  threshold old proofs never cross; passive drain of the GAUGE is
  fine as long as no old assert reads the new slots and no old
  route's behavior shifts; if that proves impossible, re-pin honestly
  with the reason recorded; (3) the 💡 lead: define the light circle
  around the player and let the slice-3 safe-radius proof bound
  first-frame darkness contact; (4) SELECT is in the ARM7 exit combo
  (never script SELECT together with START+L+R); (5) keypad_update(0)
  boot trap, pad_seen_idle guard, ±6-frame skew envelope, GL_STRESS
  `make clean` discipline, py-desmume BGR swap all still apply.
- Session 26 was a merged-seat BOOT heartbeat (control/status.md
  overwrite, coordinator side); sessions 22/23 were bare hourly wakes.
- Sibling worker concurrently active in this container (Brineward
  port+upgrades, session 28; their PR #61 expected to land during
  this session). This slice works in its OWN fresh clone
  (`/home/user/gl-slice7`), touches zero Brineward files, and keeps
  its `rom-builds.yml` / `dist/README.md` diffs to the Gloamline
  sections only; if their PR merges first, rebase + re-run proofs.
- Coordinator constraint for this seat (unchanged from session 24):
  `control/**` is coordinator-only — no claim file and no status.md
  edit from this worker; flagged here instead, for the lane's next
  wake to reconcile.
- Coordinator policy update mid-brief: the PR is to be parked READY
  (not draft) with ALL checks completed green — the merge click is
  the owner's. No self-merge this session.

## What this session did

(in progress — born red; filled at close-out)
