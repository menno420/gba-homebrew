# Session 28 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-12 (branch `claude/brineward-port-upgrades`)
- mission: **BRINEWARD arc slice 4 — PORT + UPGRADES.** Coordinator-assigned:
  roadmap item 2 from `docs/concepts/brineward-concept.md` (loot/gold →
  **port+upgrades** → the Maw → …): spend BANKED gold at the Graywake
  pier on persistent ship upgrades — the three tracks (hull / cannons /
  sails, tiers I/II/III) plus repair costs. Rails carried from slice 3:
  pure sim `bw_sim.{h,c}` mirrored by `tools/check-brine.py` (lockstep
  rule), telemetry mailbox EXTENDED (words 0-19 pinned, never re-map),
  pinned headless proofs in the `nds-brineward-build` CI job (Gloamline's
  job + the GBA gate untouched), `dist/brineward.nds` refresh,
  byte-deterministic build.
- session number 28 claimed (session 25 = Brineward slice 3, PR #61;
  session 26 = coordinator seat; session 27 = the Gloamline lantern-oil
  sibling running concurrently in this container).
- **📊 Model:** fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)
- process note: `control/` is coordinator-only (2026-07-11 restructure) —
  no claim file or status write rides this PR; this line records the
  deliberate omission, as sessions 24/25 did.

## 💡 Session idea

(placeholder — filled at close-out)

## Previous-session review

(placeholder — filled at close-out)

## What this session did

(in progress)
