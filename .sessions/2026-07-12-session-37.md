# Session 37 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-12 (branch `claude/gloamline-watch-map`)
- mission: **Gloamline arc slice 10 — WATCH-MAP POLISH.** The last
  remaining item on the concept doc's LATER SLICES list
  (docs/concepts/gloamline-concept.md: "shove verb + multi-zombie
  waves; night ramp/plateau; barricades; between-nights scavenge
  interlude; lantern-oil light pressure; synthesized audio set;
  save-file best-nights; **watch-map polish**" — slices 4-9 shipped
  everything before it). Scope, honoring the concept's own watch-map
  words ("bottom = the watch-map — dawn clock, night number, zombie
  dots, later the oil gauge. A pure render of the same state...
  Touch optional, never required (tap the map to drop a marker at
  most); the game is 100% playable on buttons"):
  (1) a **chalk mark** the lamplighter drops on the watch-map — X
  places it at your feet / clears it (buttons-first, BINDING), and
  tapping the map plot is the optional touch alias that drops or
  moves it to the tapped cell; the mark is pure map state (the dead
  ignore chalk — zero sim feedback, so every pre-slice-10 pin holds
  bit-identically), persists across nights within a run, clears on a
  fresh run; (2) a **watch line** on the map: OUT — how many of
  tonight's dead are still out in the gloam (pure
  gl_gloam_out(wave_total, spawned)) — plus the mark's range when
  the chalk is down; (3) the **best-night record on the map header**
  (render of the existing slice-9 state — what you're chasing,
  visible while you play). Pure layer: the map cell geometry moves
  verbatim from main.c statics into gl_sim (gl_map_col/gl_map_row)
  plus gl_mark_of_cell / gl_mark_of_touch (mid-span inverse with an
  EXACT cell round-trip, proved host-side) and gl_gloam_out;
  three-way lockstep (gl_sim.c <-> tools/check-gloam.py <->
  tools/gloam-route.py); telemetry appends slots 64-71 with 0-63
  frozen (the mailbox grows 64 -> 72 words); zero-re-pin shape
  targeted (an 8th hold); tools/nds-headless-check.py gains a
  `--touch START-END:X,Y` span so the touch alias is provable
  headlessly; 3 new pinned headless proofs (mark by buttons incl.
  clear-on-fresh-run, mark by touch incl. off-plot rejection, the
  OUT countdown + cross-night mark persistence on the committed
  night-2 route). The save format is NOT touched. 100% original
  content. This session runs on a coordinator-seat dispatch that
  sanctions the claim file and a status.md heartbeat APPEND (the
  sessions 24-31 "coordinator-only control/**" constraint is lifted
  for this dispatch by its own terms).
- 📊 Model: fable-5 (family-level self-report from this session's own
  harness/environment banner, per ORDER 003)

## 💡 Session idea

(interim — to be written at close-out with the slice's real
transferable lesson; current candidate: designing an inverse mapping
as "mid-span of the forward map's preimage" makes the round-trip an
exactly provable property instead of a ±1-cell approximation.)

## Previous-session review

- Session 36 (PR #73 lineage, squash-merge heartbeat precedent) was
  the WORK-LOOP cycle-5 coordinator heartbeat: control/status.md
  overwritten wholesale, no game files. Its status body is now STALE
  against HEAD `4ad557d`: it lists PR #68 (Gloamline slice 8) and
  PR #69 (Brineward slice 5) as "parked READY+green awaiting owner
  clicks" — both have since MERGED (slice 8 at `27e8851`, Brineward
  slice 5 at `a5b5325`), and the queued slice 9 (session 33) also
  landed (`4ad557d`, PR #74). This session's heartbeat APPEND (a
  dated section, not an overwrite — per the dispatch) records the
  staleness rather than silently rewriting it.
- Session 33 (slice 9, the structural template for this card) set the
  rails this slice builds on: telemetry slots 0-63 now frozen (new
  slots >= 64), the keypad_update(0) boot trap + pad_seen_idle guard,
  the +-6-frame movement-skew envelope, GL_STRESS `make clean`
  discipline, mirror-predict-then-pin for every emulator literal, and
  the 7th consecutive zero-re-pin hold (23 proofs / 399 asserts) that
  this slice must extend, not break.
- At session start: main = `4ad557d` (slice 9 merge), 0 open PRs,
  control/claims/ empty (README only) — watch-map unclaimed; branch
  `claude/brineward-wind` exists pre-built and PR-less (sibling arc,
  untouched by this session).

## What this session did

(interim — completed at close-out with commits, PR number, CI run
ids, dist sha256, proof/assert counts and the re-measured frame
budget. Plan of record: born-red card + claim first; pure layer +
mirrors in lockstep; main.c glue; harness --touch; 3 pinned proofs
in the NDS ROM build job; local full verify incl. GL_STRESS
re-measure and byte-determinism double build; dist + docs refresh;
heartbeat append; flip + claim delete last.)

## Anomaly log (for the coordinator + next wake)

(interim — walls hit and what green does NOT verify, written at
close-out.)
