# Session 49 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-13 (branch `claude/shoal-levels`, started 21:28Z)
- mission: **SHOAL growth rung 4 — MULTIPLE TUNED LEVELS.** The next
  named deliberate cut in `games/shoal/CONCEPT.md` @ main `b04be5a`
  ("Deliberately still cut: multiple tuned levels, audio") — the
  committed concept's S3 scope names it "4 levels with tuned flock
  parameters + rating screen" (`docs/concepts/session-1-concepts.md`).
  Levels only — audio stays cut.
- dedup at claim time: no shoal claim in `control/claims/`, no open
  Shoal PR (search `is:open shoal` = 0); inbox at HEAD unchanged —
  ORDERS 001-005 all served, nothing new unserved.
- knob rule for this rung (coordinator-authorized): PER-LEVEL retunes
  of the coupled triple (straggler / den / goal) are allowed, but
  only all-three-together, deliberately, justified here — and every
  level's win route is derived fresh against its own parameter set
  (routes re-derived, never just re-pinned). The carried waters
  (START calm / SELECT hungry / R gated) stay byte-identical — the
  levels live on their own verb.
- landing posture: PR opened READY on main immediately after this
  born-red commit; no merge/approve/auto-merge calls from this
  session — the server-side enabler decides on green.

(Body lands at the flip. substrate-gate holds red on this badge until
then — the designed born-red hold; nothing to investigate.)
