# Session 46 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-13 (branch `claude/shoal-predator`, started 20:04Z)
- mission: **SHOAL growth rung 1 — THE PREDATOR PASS.** The first
  named deliberate cut in `games/shoal/CONCEPT.md` @ main `e59b26d`
  ("Deliberately cut (the committed concept's later rungs):
  **predators locking onto stragglers**, gates, star ratings,
  multiple tuned levels, audio") — and only that item; gates/ratings/
  levels/audio stay cut. Coordinator constraint honored: a second
  flock does NOT fit the budget and is not attempted.
- budget rails (session-45 guard recipe): the carried `t[5] < 4096`
  regression pin must hold with ZERO re-pins on all carried asserts;
  a new equivalent budget pin covers predator-live frames; the new
  mean/worst/p99 measured the same way as the gate run.
- gate plan (the lane's input-verb gate, Brineward slice-7 pedigree):
  predators live only in a run started with SELECT ("hungry water");
  START keeps the calm water — no committed script presses SELECT,
  so every carried proof is bit-identical by construction. Telemetry
  extends 16 → 24 (words 0-15 frozen, append-never-remap).
- dedup at claim time: no shoal claim, no open Shoal PR; inbox at
  HEAD still ORDERS 001-005 (nothing new unserved). Session 46
  claimed (45 = Shoal, PR #98, merged `e59b26d`).
- **📊 Model:** Claude 5 family (Fable) — family-level self-report
  from this session's own harness banner, per ORDER 003.
- landing posture: PR opens READY on main; no merge/approve/
  auto-merge calls from this session — the server-side enabler
  decides on green.

(Close-out, measured numbers, idea, and previous-session review land
at the flip.)
