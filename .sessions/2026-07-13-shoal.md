# Session 45 — game-lab Track B

> **Status:** `in-progress`

- date: 2026-07-13 (branch `claude/shoal`, started 19:26Z)
- mission: **SHOAL — profiling-gated prototype slice.** The LAST
  unbuilt item of the committed ORDER-001 concept queue
  (`docs/concepts/session-1-concepts.md` @ main `70e3e3c`): "You
  steer a current (a cursor that pushes water) to herd a school of
  40-60 fish past predators, through gates, into the safe reef."
  Risk MEDIUM-HIGH with the concept's own committed gate, honored
  verbatim this session: "The flock is the game — if 50 boids don't
  fit the CPU/OAM budget the concept degrades fast (mitigations:
  neighbor grids, 16.16→8.8 fixed precision drop, flock size
  tuning)... **profile in the FIRST session, pivot cheap if the
  budget says no.**"
- gate plan: PROFILE FIRST — 50 boids (mid of the concept's 40-60),
  full O(n^2) cohesion/alignment/avoidance in 8.8 fixed point,
  measured on emulated hardware via Butano's core CPU API
  (`bn::core::last_cpu_usage/last_cpu_ticks` — the "Butano profiler
  from session 1" the concept cites), surfaced through the telemetry
  mailbox so the headless harness PINS the measured cost. Budget
  holds → ship the boid slice; budget fails → apply the concept's own
  mitigation ladder in its written order, and if the ladder fails
  too, ship the measured verdict + the cheap pivot. Numbers cited
  either way.
- dedup at claim time: no shoal in games/, no open PR, no claim;
  inbox at HEAD still ORDERS 001-005 (no new unserved order).
- session number 45 claimed (44 = Clockwork Courier, PR #96, merged
  `70e3e3c`).
- **📊 Model:** Claude 5 family (Fable) — family-level self-report
  from this session's own harness banner, per ORDER 003.
- landing posture: PR opens READY on main; no merge/approve/
  auto-merge calls from this session — the server-side enabler
  decides on green.

(Close-out, verdict numbers, idea, and previous-session review land
at the flip.)
