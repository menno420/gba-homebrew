# claim — skeleton/track-b-walking-skeleton

- `skeleton/track-b-walking-skeleton` · session-1 walking skeleton (kit
  adoption + control heartbeat, then toolchain install + Butano example +
  headless screenshot per ORDER 001) · expected files/area: `.sessions/`,
  `control/status.md`, `bootstrap.py` + kit-planted docs, toolchain tree,
  `docs/` (concepts) · Fri Jul 10 02:05:42 UTC 2026 (`date -u`)

*(Session 1 spans two worker phases; the phase-2 worker inherits this claim
and deletes it at session close per `claims/README.md`.)*
