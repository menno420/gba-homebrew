- `claude/wickroad-master-tier` · **Wickroad run-tier MASTER tier — descending first-match tier table on the title (runs>=100)** — extends the persistent title-screen tier tag (#190) from a single tier to a descending first-match table: runs >= 100 → "MASTER", 50..99 → "VETERAN", < 50 → nothing. Still a persistent LEVEL keyed on the CURRENT stored lifetime run count (the value #186 renders as `RUNS n`), read every boot, first-match-wins so the highest earned tier shows. Pure header helper `wr::run_tier_label` (no Butano/GBA headers, mirrored by stdlib host test `tools/check-run-tier.py` with pinned boundaries 49/50/51/99/100/101); no new render plumbing (main.cpp already prints whatever the helper returns); gated on `ledger_loaded` so the no-save path stays byte-identical; zero new RNG/telemetry/art/ledger delta. Follows #184/#185/#186/#189/#190. · games/wickroad/src/wr_milestones.h · tools/check-run-tier.py · 2026-07-18
