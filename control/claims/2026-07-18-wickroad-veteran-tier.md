- `claude/wickroad-veteran-tier` · **Wickroad persistent VETERAN tier tag on title screen (runs>=50)** — once the SRAM ledger's lifetime run count reaches a tier threshold, the TITLE screen shows a persistent tier tag beside #186's existing `RUNS n` line (runs >= 50 → "VETERAN"). Unlike #189's transient end-card milestone flash, this reads the CURRENT stored lifetime total (a persistent LEVEL, not a crossing event) so it re-shows every boot. Decision extracted to a SECOND pure header helper (`wr::run_tier_label`, no Butano/GBA headers, separate concern from `run_milestone_label`) mirrored by a stdlib host test (`tools/check-run-tier.py`); gated on `ledger_loaded` so the no-save path stays byte-identical; zero new RNG/telemetry/art/ledger delta. Follows #184/#185/#186/#189. · games/wickroad/src/main.cpp · games/wickroad/src/wr_milestones.h · tools/check-run-tier.py · 2026-07-18
