- `claude/tiltstone-arc2-cut5` · **Tiltstone arc 2, cut 5 — «The monotone difficulty ramp» (arc closer)** — an OPTIONAL difficulty floor on generation: `generateLevel(seed, levelIndex, opts)` gains a third arg — when `opts.floor` is a finite number it re-salts until `difficulty(candidate).score` clears it (absent → byte-identical to before, same RNG stream / first-valid salt); pure `rampFloor(scores)` (running-max floor, 0 on empty) + `generateRamp(seed, count, {monotone})` chain builder that floors each level against the hardest score so far, yielding a chain provably non-decreasing in `difficulty().score` (seed 42 x6 lifts the natural LV4 dip 5991→8991, LV5 6992→9991). Default output unchanged; floor-off generation and today's daily chain stay byte-identical (no pinned level moves). §18 smoke (rampFloor truths, 14×6 floor-off byte-identity, natural-chain dip pin, monotone chain scores/floors/pars pinned, non-decreasing + floor-clear + pointwise-dominance, deterministic, honest floor-exhaustion throw), engine bumped 1.7.0→1.8.0 · games/web-tiltstone/{engine.js,smoke.mjs}, dist/web/tiltstone/engine.js, docs/arcs/TILTSTONE.md, docs/current-state.md · 2026-07-16
