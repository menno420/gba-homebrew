- `claude/underroot-slice-10` · **Underroot arc slice 10 — Seed dial + balance (pick/scan the year seed; a difficulty/escalation pass): a runtime SEED DIAL (LEFT/RIGHT) picks one of UR_DIAL_COUNT deterministic year seeds — dial 0 == UR_SEED so every slices-1..9 proof stays bit-identical — and the dialed run seed feeds the whole f(seed,season,index) schedule; a pure REFERENCE survivable plan proves the balance invariant "every dial seed is survivable" (winter score >= UR_FAIR_FLOOR > 0 — no seed is a death-trap) with a real difficulty spread across the dial; and slice-9's forward note is honored — the START record-commit is now GATED on a realized winter (season == UR_WINTER), so the persisted best is an outcome the colony actually reached, not a spring forecast** + host mirror `prove_balance` (seed->schedule wiring pure, dial seeds distinct, every dial seed fair, difficulty spread) + ROM seed-select telemetry (dial/run-seed/fair/ref-score) + winter-gated save roundtrip (spring START blocked, winter START commits score 3003 season 3) · games/underroot-nds/, tools/check-underroot.py, .github/workflows/rom-builds.yml · 2026-07-16
