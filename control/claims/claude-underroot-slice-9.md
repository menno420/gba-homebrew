- `claude/underroot-slice-9` · **Underroot arc slice 9 — EEPROM best (best SCORE / furthest season persisted via card backup): a fixed 32-byte record { magic, version, best_score, furthest_season, run_seed, spares, checksum } round-trips through the emulated cartridge backup; a corrupt/blank/future-version blob decodes to the fresh table (never a crash); the record commits on a discrete START press ONLY when it strictly improves (wear discipline); host mirror `prove_save` (encode/decode roundtrip + golden bytes + every bad blob rejects) + ROM `--battery-in/out` cycle (fresh chip -> one write -> bytes == mirror encode; power cycle loads the record; equal run writes nothing; corrupt/version-bump = fresh table)** + tools/underroot-save.py + host mirror `ur_save_*`/`ur_record_improves` · games/underroot-nds/, tools/check-underroot.py, tools/underroot-save.py, .github/workflows/rom-builds.yml · 2026-07-16
