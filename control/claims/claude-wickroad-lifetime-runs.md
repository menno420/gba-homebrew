- `claude/wickroad-lifetime-runs` · **Wickroad: surface the lifetime run count on the title — `best.runs` is banked in SRAM (incremented in record_run at every run-end since crossroads cut 4) but rendered on zero screens; append `RUNS n` to the existing title BEST GOLD x DAY y line, gated on `ledger_loaded` so the no-save path is byte-identical, render-only, zero RNG/telemetry delta. Follows #184/#185.** · games/wickroad/src/main.cpp · 2026-07-18
