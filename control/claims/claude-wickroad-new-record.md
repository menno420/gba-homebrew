- `claude/wickroad-new-record` · **Wickroad: NEW RECORD flash on the end card — when a returning player's just-finished run beats their SRAM-persisted best (gold or day reached), the end card flashes NEW RECORD; comparison captured BEFORE record_run() overwrites the best, render-only, gated on `ledger_loaded` so the no-save path is byte-identical, zero RNG/telemetry delta. Follows #184.** · games/wickroad/src/main.cpp · 2026-07-18
