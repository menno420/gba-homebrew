# gba-homebrew (game-lab Track B) · status

updated: 2026-07-11T02:08:30Z
phase: session 8 slice 3 (same worker continuing) — narrowed review-queue row #23 FIXED and closed (echo-band crystal gates: neighbour-aware junction guard + full-period host proof in per-PR CI) + Lumen Drift is now DOWNLOAD-AND-PLAY (`dist/lumen-drift.gba` + `docs/PLAYING.md`). Review queue is EMPTY. Lumen Drift stays SCOPE-COMPLETE (the fix is a playability-defect repair in endless content, not new scope).
health: green
kit: v1.8.0 · check: green (full --strict exit 0 at close) · engaged: yes
boot: synced to origin/main `6565458` (post PR #30); slice heartbeat commit `8496fea` (card re-opened born-red + work claim) pushed before work per rule 7; inbox re-read at HEAD at open AND immediately before this write — no orders beyond 001/002; toolchain verified live in this container (devkitARM r68 gcc 16.1.0, python 3.11.15 + mgba 0.10.2 binding OK)
last-shipped: this slice's PR (branch `claude/session-8-slice-3`, opened READY at close; merge state lands in the git record) — (a) **Row #23 CLOSED with fix**: root cause = `row_features` places each 2-cell crystal cluster edge-only from its OWN row's layout, so at echo-band width changes the columns open in BOTH consecutive rows can shrink to exactly those 2 cells → unavoidable death gate. True extent: 126 gates over two full 10080-row echo periods (slice 2's "6 in rows 191–1093" was its ≤1200 scan window). Fix: pure neighbour-aware guard — a crystal cell is kept only if a crystal-free shared-open column survives toward both neighbours; host-proven no-op on rows 0–130 (cell-for-cell identical — both replay tiers untouched). Proof: NEW `tools/check-cave.py` (stdlib, ~0.3s, self-verifies echo periodicity then scans rows 0–20226 = every row the cave will ever generate): **0 solidity breaks, 0 crystal gates** (pre-fix mirror: 126); wired into the per-PR "ROM builds" gate. Canonical replay, deep run (DEPTH 95), SRAM two-boot and 300-frame boot all re-run green on the fixed build in THIS container. Build: `lumen-drift.gba` 163,572 B sha256 `88d8146a066d15ee89533e0af80c216eea040ce2027e810182a54f2a5216b697` (skeleton unchanged). (b) **Download-and-play**: `dist/lumen-drift.gba` committed (original content = publish-safe; supersedes the old "ROMs stay uncommitted" note by coordinator direction) + `dist/README.md` provenance (sha256, source tree, build recipe) + `docs/PLAYING.md` (mGBA desktop/phone setup, controls, goal, DEPTH/BEST/SPARKS) + README "▶ Play it" links.
blockers: none
orders: acked=001,002 done=001 (this repo's half), 002 EXECUTED (slice 2) — trigger `trig_0137SkvhXEJvwepX8aVNkcSn` "gba-homebrew hourly wake (ORDER 002)", cron `0 * * * *`, fresh session per fire in env `env_014P62UXP7cuK1bPPWWzg521`; scheduler-reported first fire was `next_run_at 2026-07-11T02:07:07Z` — **first-fire CONFIRMATION still pending** (this slice closed at ~02:08 and cannot see into the fired session; the woken session's own status/PR trail is the confirmation artifact — next wake must record it here)
review-queue: **EMPTY** — narrowed #23 fixed and closed this slice (evidence on the session-8 card, slice 3). Healthy state per the ledger's own definition.
kit-follow-ups carried (still unclaimed): `upgrade --apply-docs` (14 template-improved docs), migrate/retire legacy root `claims/` (only its README remains), auto-merge enabler wiring decision (`adopt --wire-enforcement`), upstream the PR #5 born-red-card gate rule to substrate-kit
⚑ needs-owner (all carried):
- **Concept pick** — pick 1 of the 3 committed Track B concepts (Lumen Drift-deepening / Clockwork Courier / Shoal; `docs/concepts/session-1-concepts.md`). The coordinator has PUT THE MENU TO THE OWNER — awaiting the owner's answer via any signal (inbox order or PR comment). Gates which game gets the remaining sessions. Note: row #23 is no longer gated on this (fixed this slice as a playability defect).
- **⚑ owner-click: create the Lumen Drift GitHub Release** (worker seat gets 403 on tags/releases). Exact clicks: repo → Releases → "Draft a new release" → "Choose a tag" → type `lumen-drift-v1.0` (create on publish, target `main` after this PR merges) → title `Lumen Drift v1.0` → attach the file `dist/lumen-drift.gba` from the merged tree (163,572 B; sha256 `88d8146a066d15ee89533e0af80c216eea040ce2027e810182a54f2a5216b697` — paste it in the notes) → point the notes at `docs/PLAYING.md` → Publish.
- **⚑ owner-click: merged-branch cleanup** — worker seat gets 403 on branch-delete; merged `claude/*` branches (through PR #30 + this slice's) can be deleted in one sweep from the branches page.
notes: worker seat (no tag/release/branch-delete perms — queued above). Walls carried unchanged: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA python core.load_save() segfault (--savefile bus-copy is the working path, re-exercised green this slice); devkitPro official infra Cloudflare-403 (leseratte10 mirror + SHA-256 pins). dist/ ROM convention NEW this slice: player-facing games ship committed in `dist/` with provenance; skeleton stays CI-artifact-only; CI still sha256-logs every from-source build for cross-checking.
