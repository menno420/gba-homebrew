# gba-homebrew (game-lab Track B) · status

updated: 2026-07-10T07:14:30Z
phase: session 7 COMPLETE — Lumen Drift polish pass 3 (more cave beyond row 62, PR #23): the concept doc's polish list is EXHAUSTED and Lumen Drift is marked SCOPE-COMPLETE in docs/concepts/session-1-concepts.md
health: green
kit: v1.6.0 · check: green (full --strict exit 0 at close) · engaged: yes
last-shipped: PR #22 — session-7 heartbeat (merge `cbba63b`); PR #23 — Lumen Drift polish pass 3 (merge `f502147`): the cave no longer ends at the row-62 floor — every row is a pure function of the world row and the stored 64-row map slides as a window over an endless deterministic cave (rows 0-61 byte-identical to the committed layout; 20-row echo bands cycle the three section looks forever past row 64 with their own banners); position/depth telemetry (gl_audio_hook slots 4-6); deterministic autopilot-recorded deep run to DEPTH 95 committed as docs/proof/session-7-deep-run-keys.txt and replayed in CI with numeric depth assertions; canonical replay re-bisected to +4 (the new rebake is 2 frames faster; zero overruns, loop-tick lag constant 19)
blockers: none
orders: acked=001 done=001 (this repo's half; joint done-when closes when Track A's half lands in pokemon-mod-lab) · no new orders at HEAD (inbox re-read 2026-07-10T07:14Z, HEAD `f502147`) · session 7 ran the standing default under coordinator direction: the LAST concept-doc polish item (claim released at close)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md — **Lumen Drift is now SCOPE-COMPLETE: committed scope (3 sessions) + the ENTIRE polish list (title, SRAM best, audio, light-radius mechanic, tuning, cave beyond row 62) built and proven; the polish list is exhausted, so no further Lumen Drift work is queued.** HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions — with the polish list done the pick is a pure fork: keep investing in Lumen Drift (new scope needs owner say-so) or transfer the scaffolding to Clockwork Courier / Shoal with zero sunk work. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. NOTE: everything generic transfers (games/common/ input/kinematics/collision/run-state/save/light-window/audio-hook headers, harness scripting + text/watch assertions + savefile persistence + telemetry-driven route recording, all-games CI). UNBLOCKS: the next build target for Track B's remaining sessions.
notes: replay timeline re-bisected in PR #23: the sliding-window rebake is FASTER than the old full-array rebake, so the canonical script moved +5 → +4 (guard recipe extended on the session-7 card: the bisect plateau is non-contiguous — validate the full assert suite, don't reason from lag alone; the deep-run script is per-frame with no settles and must be RE-RECORDED, not shifted, if timing changes). Post-merge dispatch of headless-boot.yml on `f502147` = run 29076028307 (green confirmation in review row #23). Walls carried: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA core.load_save() segfault (PLATFORM-LIMITS.md — --savefile bus-copy is the working path). Supply chain CLOSED to trust-on-first-use (PR #13 pins). Post-merge review rows queued: docs/review-queue.md #12, #13, #16, #20, #23.
