# gba-homebrew (game-lab Track B) · status

updated: 2026-07-11T01:49:30Z
phase: session 8 slice 2 (same worker continuing) — ORDER 002 executed + review-queue burn-down: 11 of 12 open rows verified and closed, row #23 narrowed to one real finding, two engine defects found AND fixed. Lumen Drift stays SCOPE-COMPLETE (fixes are engine-correctness only; both CI replay tiers re-run locally, green).
health: green
kit: v1.8.0 · check: green (full --strict exit 0 at close) · engaged: yes
boot: synced to origin/main `827a466` (post PR #29); slice heartbeat commit `e70c5c7` (card re-opened born-red + work claim) pushed before work per rule 7; inbox re-read at HEAD at open AND immediately before this write — no orders beyond 001/002
last-shipped: this slice's PR (branch `claude/session-8-review-queue`, opened READY at close; merge state lands in the git record) — ORDER 002 + review-queue burn-down. (a) Rows #3 #5 #6 #8 #9 #12 #13 #16 #17 #20 #29 CLOSED with in-container evidence (verdicts on the session-8 card): mirror pins re-derived live (2/2 match), gate rule proven both directions locally + PR #8's CI log quoted, assets proven original by byte-exact regeneration (all 7 binaries), tunnel continuity host-verified rows 5–1200, both replay tiers + the SRAM two-boot proof re-run green on today's build. (b) Two real defects fixed: gl_tile_grid `_cell()` negative-coordinate sliver (bn::fixed division truncates toward zero — host-proven 35 mispredictions, 0 after; positive coords bit-identical) and gl_light_window `_half_widths` OOB read for off-screen centers with radius > 160. (c) tools/build.sh floor aligned to CI (`-ge 2`). (d) Row #23 NARROWED to the one thing that failed re-check: 6 echo-band junctions (rows 191–1093) where every shared open column is crystal'd = unavoidable death gates in endless content (committed rows 0–63 clean); fix is its own slice, sensibly post concept-pick. Build after fixes: lumen-drift.gba 163,380 B sha256 `4db9f791…4729f74`, skeleton.gba 114,464 B (unchanged).
blockers: none
orders: acked=001,002 done=001 (this repo's half), **002 EXECUTED this slice** — mechanism verbatim: `claude-code-remote` MCP `create_trigger`; cross-session bind refused (`create_trigger: binding a trigger to another session is not enabled for this organization. Omit persistent_session_id to bind to the calling session, or set create_new_session_on_fire=true.`); armed via its fallback: trigger `trig_0137SkvhXEJvwepX8aVNkcSn` "gba-homebrew hourly wake (ORDER 002)", cron `0 * * * *`, fresh session per fire in env `env_014P62UXP7cuK1bPPWWzg521`, prompt exactly as ordered ("Read control/inbox.md at HEAD and run the standing ritual from your instructions."), scheduler-reported first fire `next_run_at 2026-07-11T02:07:07Z` — first-fire CONFIRMATION is pending (fires after this close); the next wake must record it here
review-queue: ONE row open — narrowed #23 (echo-band crystal gates, evidence + fix recipe on the session-8 card). Rows #3–#20, #29 closed 2026-07-11 (git history archives the old rows; verdicts on the card)
kit-follow-ups carried (still unclaimed): `upgrade --apply-docs` (14 template-improved docs), migrate/retire legacy root `claims/` (only its README remains), auto-merge enabler wiring decision (`adopt --wire-enforcement`), NEW: upstream the PR #5 born-red-card gate rule to substrate-kit (now proven both directions in-container)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md — Lumen Drift is SCOPE-COMPLETE. HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions, and gates the narrowed row #23 fix (endless-content hazard rewrite only pays off if Lumen Drift is the pick). VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. UNBLOCKS: the next build target for Track B's remaining sessions + the row #23 crystal-gate fix.
notes: worker seat (no tag/release/branch-delete perms — none needed; no ⚑ owner-click items queued this slice). Walls carried unchanged: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA python core.load_save() segfault (--savefile bus-copy is the working path, re-exercised green this slice); devkitPro official infra Cloudflare-403 (leseratte10 mirror + SHA-256 pins, two pins re-verified live today). ROMs stay uncommitted; CI artifact step gives every commit a sha256-logged ROM. 💡 carried: in-repo `tools/check-cave.py` (the host-side continuity/crystal checker from this slice's scratchpad) would pin the passability contract in CI for seconds per run.
