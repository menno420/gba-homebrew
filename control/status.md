# gba-homebrew (game-lab Track B) · status

updated: 2026-07-11T03:40:00Z
phase: session 8 slice 6 (same worker continuing) — **CONSOLIDATION shipped as Lumen Drift v1.3**: slice 5's ranked polish debt items 1–3 all paid (route-recorder promoted into the repo, graze visual tell, pause/mute); item 4 (graze hand-feel) stays owner-gated. `dist/lumen-drift.gba` refreshed to v1.3.
health: green
kit: v1.8.0 · check: green (full --strict exit 0 at close) · engaged: yes
boot: synced to origin/main `177f6b9` (post PR #33, v1.2 "graze the light"); slice heartbeat commit `53ac8e1` (card re-opened born-red + work claim) pushed before work per rule 7; toolchain verified live in this container (devkitARM r68, python 3.11.15 + mgba 0.10.2 binding OK; baseline build reproduced slice 5's v1.2 sha256 `e2af7daa…` exactly before any change)
last-shipped: this slice's PR (branch `claude/session-8-slice-6`, opened READY at close; merge state lands in the git record) — **v1.3 consolidation, three items:** (1) **ROUTE-RECORDER PROMOTED** — the closed-loop autopilot sessions 7/8 rebuilt three times in scratchpad is now `tools/route-recorder.py` (corridor planning from the pure-layout mirror in `tools/check-cave.py`, braking-distance fall governor, full docstring contract + main.cpp lockstep note); the harness gained `--keys-file` (both recorded-route CI steps now use it) and `--assert-pixels FRAME:R,G,B:OP:COUNT`. DETERMINISM PROVEN THE STRONG WAY: on the final v1.3 build, `route-recorder.py 185 3200 OUT` regenerated the committed `docs/proof/session-8-tier-run-keys.txt` keys line **byte-identically** (diff clean in THIS container; args pinned in the workflow comment) — future proofs record routes instead of rebuilding the pilot. (2) **GRAZE VISUAL TELL** — the mote itself flashes hot for 6 frames the instant a graze pays (sprite-palette fade to reserved `bn::color(31,31,20)` = mGBA RGB 255,255,165; verified absent from EVERY committed proof screenshot); CI graze step pixel-asserts 0/164/0/164/0 across pre-graze, both payout flashes, between, and the game-over card. (3) **PAUSE/MUTE** — START pauses a live run (physics, surge, light decay AND telemetry provably frozen; B from the pause card restarts; START's old mid-run hard-restart removed — no committed replay ever pressed START mid-run, verified), SELECT toggles mute anywhere (gates play() only; hook counters keep counting). NEW dispatch-tier CI step proves both in one no-input fall: hook[9] holds exactly 95 across 265 paused frames, surge frozen (alive at f399 where the unpaused baseline dies ~356), muted death at f633 with mixer SILENT (vs SRAM tier's voiced f360 death), unmute+restart voiced again (hook[0] eq 2 at f724, mix gt 0 at f730); a second run proves B-from-pause restarts. PROOF SWEEP: ALL replay tiers green with ZERO expectation edits (canonical, deep-run via --keys-file, tier-run via --keys-file, graze + 5 new pixel asserts, SRAM two-boot, 300-frame boot, 2 new pause/mute runs); `check-cave.py` UNCHANGED by design and re-run green (rows 0..20405, 0 breaks/gates, tripwire 20/22/27/33). Build: `lumen-drift.gba` **167,996 B** sha256 `195a86795e57e2fa0059a96782f1ac7a147cbcebc0cb28a96f353e5d9babae94` (skeleton untouched). Dist: `dist/lumen-drift.gba` = this exact build, provenance row updated; `docs/PLAYING.md` controls table now lists pause/B-restart/mute + the flash; `docs/current-state.md` gains the v1.3 ledger entry and a route-recorder stability-baseline entry. Proof shots committed: `docs/proof/session-8-graze-flash.png`, `session-8-pause-card.png`.
blockers: none
orders: acked=001,002 done=001 (this repo's half), 002 EXECUTED (slice 2) — trigger `trig_0137SkvhXEJvwepX8aVNkcSn` "gba-homebrew hourly wake (ORDER 002)", cron `0 * * * *`, fresh session per fire in env `env_014P62UXP7cuK1bPPWWzg521`; **first-fire confirmation STILL PENDING** (scheduler-reported first fire was `next_run_at 2026-07-11T02:07:07Z`; slices 4–6 ran in the coordinator-driven worker session 02:15–03:40 and cannot see into fired sessions — the woken session's own status/PR trail is the confirmation artifact; next wake must record it here)
review-queue: EMPTY (since slice 3).
lane position (honest): **the local backlog is now empty or owner-gated.** Slice 5's polish debt items 1–3 are PAID this slice; what remains is (a) the ⚑ owner items below (concept pick, release click, branch cleanup, graze hand-feel), and (b) the low-priority kit follow-ups carried since the v1.8.0 upgrade. No undone game work is known: v1.0 scope complete, v1.1–v1.3 deepening/polish shipped and proven. **Lane idle pending owner input; the hourly wake trigger continues the ritual** (inbox re-read + standing default) — the standing default's "next playable increment" is now genuinely gated on the owner's concept pick rather than on unfinished work.
polish-debt: items 1–3 PAID (this slice). Item 4 carried as ⚑ (owner hands-on graze tuning below). Kit follow-ups still carried (next line).
kit-follow-ups carried (still unclaimed, low priority): `upgrade --apply-docs` (14 template-improved docs), migrate/retire legacy root `claims/` (only its README remains), auto-merge enabler wiring decision (`adopt --wire-enforcement`), upstream the PR #5 born-red-card gate rule to substrate-kit
⚑ needs-owner (all carried):
- **Concept pick** — pick 1 of the 3 committed Track B concepts (Lumen Drift-deepening / Clockwork Courier / Shoal; `docs/concepts/session-1-concepts.md`). The menu is WITH THE OWNER; slices 4–6 executed the coordinator's announced default (deepen Lumen Drift). An explicit pick still gates the remaining sessions.
- **⚑ owner-click: create the Lumen Drift GitHub Release** (worker seat gets 403 on tags/releases). v1.3 SUPERSEDES the v1.2 tag suggestion once this slice's PR merges — suggested tag now **`lumen-drift-v1.3`**. Exact clicks: repo → Releases → "Draft a new release" → "Choose a tag" → type `lumen-drift-v1.3` (create on publish, target `main` after this PR merges) → title `Lumen Drift v1.3` → attach `dist/lumen-drift.gba` from the merged tree (167,996 B; sha256 `195a86795e57e2fa0059a96782f1ac7a147cbcebc0cb28a96f353e5d9babae94` — paste it in the notes) → point the notes at `docs/PLAYING.md` and the v1.3 entry in `docs/current-state.md` → Publish.
- **⚑ owner-click: merged-branch cleanup** — worker seat gets 403 on branch-delete; merged `claude/*` branches (through PR #33 + this slice's) can be deleted in one sweep from the branches page.
- **⚑ NEW: graze tuning wants owner hands-on validation** — the 400-frames-per-graze refund and the 6px shell are reasoned and machine-proven (CI asserts the refund and the lane geometry), but whether grazing FEELS right — risk-priced, readable, not exploitable — needs real hands on a real run. Was slice-5 polish-debt item 4; the only non-kit work item left, and it is owner-gated by nature.
notes: worker seat (no tag/release/branch-delete perms — queued above). Walls carried unchanged: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA python core.load_save() segfault (--savefile bus-copy is the working path, re-exercised green this slice); devkitPro official infra Cloudflare-403 (leseratte10 mirror + SHA-256 pins). dist/ convention: player-facing games ship committed in `dist/` with provenance (v1.3 row live); skeleton stays CI-artifact-only; CI sha256-logs every from-source build for cross-checking.
