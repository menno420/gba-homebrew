# gba-homebrew (game-lab Track B) · status

updated: 2026-07-10T03:09:30Z
phase: session 2 complete — Lumen Drift increment 1 first-playable merged on green (PR #6, merge 72f5b04); lane idle pending owner concept pick / next order
health: green
kit: v1.6.0 · check: green (full --strict exit 0, session card complete) · engaged: yes (substrate-gate + "ROM builds" ran live on PRs #5/#6, all green; gate now handles born-red heartbeat cards — second live-fire fix, PR #5)
last-shipped: PR #6 — Lumen Drift increment 1: playable gravity cave drifter (A = thrust, D-pad steer, START restart, depth score HUD, light-decay palette trick), generic scaffolding in games/common/ (build glue + input/kinematics/tile-collision headers), headless harness gained scripted button injection + interactivity assertion, "ROM builds" CI builds ALL games (40s warm), proof PNGs docs/proof/session-2-lumen-drift-{boot,input}.png
blockers: none
orders: acked=001 done=001 (this repo's half; joint done-when closes when Track A's half lands in pokemon-mod-lab) · no new orders at HEAD (inbox re-read 2026-07-10T03:08:44Z immediately before this write) · session 2 ran the standing default: Lumen Drift increment 1 (coordinator-directed; claim released at close)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md. HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. NOTE: increment 1 was built as Lumen Drift under the standing default (coordinator directive); the reusable half (games/common/, harness input scripting, all-games CI) transfers if the owner picks differently, and the pick remains fully open. UNBLOCKS: locks the target for increment 2 (shards, death/restart, cave sections per the concept doc) and retires the generic-scaffolding hedge.
notes: Lane-written per control/README.md; inbox re-read at HEAD (unchanged since seed — ORDER 001 only) immediately before this write. Supply-chain caveat stands: devkitARM route is the unsigned leseratte10 community mirror; session-1 idea to checksum-pin the packages still open. Gate lesson worth upstreaming to substrate-kit (with the PR #3 sentinel fix): a card ADDED by a PR gates advisory (born-red heartbeat), a MODIFIED card keeps the locked door — PR #2 had merged red for lack of this. Record correction: session-1 handoff said "both PRs self-merged on green"; PR #2's substrate-gate actually concluded failure. Post-merge review rows queued: docs/review-queue.md #5, #6.
