# gba-homebrew (game-lab Track B) · status

updated: 2026-07-10T03:31:15Z
phase: session 3 complete — Lumen Drift increment 2 (hazards + fail state) merged on green (PR #9, merge d6b99ce); lane idle pending owner concept pick / next order
health: green
kit: v1.6.0 · check: green (full --strict exit 0, session card complete) · engaged: yes (substrate-gate + "ROM builds" ran live on PRs #8/#9, all green; the PR #5 born-red-card rule carried its first zero-friction heartbeat)
last-shipped: PR #9 — Lumen Drift increment 2: two hazards make depth score meaningful (crystal spike clusters on tunnel walls; the surge — an ember wall descending from the cave mouth, so stalling always loses), touch = run over, game-over card with final DEPTH + BEST (best persists across restarts in RAM), START restarts; generic gl_run_state.h (run lifecycle + best score) in games/common/; hazard sensing reuses gl_tile_grid verbatim via a second predicate; proof PNGs docs/proof/session-3-{hazards-gameplay,crystal-death,surge-closing,game-over-best}.png — one deterministic scripted run covering crystal death, restart, surge death and best-depth persistence (--require-distinct PASS)
blockers: none
orders: acked=001 done=001 (this repo's half; joint done-when closes when Track A's half lands in pokemon-mod-lab) · no new orders at HEAD (inbox re-read 2026-07-10T03:31:15Z immediately before this write, HEAD d6b99ce) · session 3 ran the standing default: Lumen Drift increment 2 (coordinator-directed; claim released at close)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md (estimates refreshed this session against measured increments 1–2). HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. NOTE: increments 1–2 were built as Lumen Drift under the standing default (coordinator directive); the generic half (games/common/ input/kinematics/collision/run-state, harness input scripting, all-games CI) transfers if the owner picks differently. UNBLOCKS: locks the target for increment 3 (Lumen Drift: shards + cave sections + SRAM best, per the refreshed concept doc; or transfers scaffolding to the picked concept) and retires the generic-scaffolding hedge.
notes: Lane-written per control/README.md; inbox re-read at HEAD (unchanged since seed — ORDER 001 only) immediately before this write. Supply-chain caveat stands: devkitARM route is the unsigned leseratte10 community mirror; checksum-pin idea now a routed backlog file (docs/ideas/toolchain-checksum-pin-2026-07-10.md). ⚑ upstream to substrate-kit still carried: both substrate-gate fixes (PR #3 no-card sentinel, PR #5 added-card-advisory) — PR #8 was the first zero-friction live run of the latter. Backlog groomed per the conveyor: 2 idea files captured+routed, concepts estimates refreshed, current-state brought current. Post-merge review rows queued: docs/review-queue.md #8, #9.
