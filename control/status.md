# gba-homebrew (game-lab Track B) · status

updated: 2026-07-10T05:48:00Z
phase: session 5 COMPLETE — phase A: both substrate-gate fixes upstreamed to menno420/substrate-kit (kit PRs #105 claim + #108 build, squash merge `2e8c0a2`, their auto-merge on green); phase B: Lumen Drift polish pass 1 (SRAM best persistence + title screen, PR #16)
health: green
kit: v1.6.0 · check: green (full --strict exit 0 at close) · engaged: yes
last-shipped: PR #15 — session-5 heartbeat; substrate-kit #105/#108 — gate fixes upstreamed (merge `2e8c0a2`); PR #16 — Lumen Drift polish pass 1 (title screen + SRAM best persistence, merge `eaefc1c`); PR #17 — headless-boot runner python pin (mgba wheel is cp311-max; first-ever dispatch of the workflow caught it)
blockers: none
orders: acked=001 done=001 (this repo's half; joint done-when closes when Track A's half lands in pokemon-mod-lab) · no new orders at HEAD (inbox re-read 2026-07-10T05:38Z, HEAD `eaefc1c`) · session 5 ran the standing default under coordinator direction: upstream gate fixes + lumen polish (claim released at close)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md — Lumen Drift carries FIRST-COMPLETE (3 sessions) + POLISH PASS 1 notes (SRAM best persistence across power cycles + title screen shipped in PR #16; still unbuilt from the polish list: audio, light-radius mechanic, tuning, cave beyond row 62). HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions — Lumen Drift is scope-complete WITH its first polish pass, so the pick decides continue-polish-vs-transfer, not sunk work. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. NOTE: everything generic transfers (games/common/ input/kinematics/collision/run-state/stage/save headers, harness scripting + text assertions + savefile persistence, all-games CI). UNBLOCKS: locks the target for further polish (Lumen Drift) or transfers the scaffolding to the picked concept.
notes: ⚑ upstream-to-kit is DONE and dropped (kit PRs #105+#108, squash `2e8c0a2` — both gate fixes now upstream; local pre-push there: 814 tests, ruff clean, strict 0). New wall recorded in PLATFORM-LIMITS.md: mGBA python binding core.load_save()+VFile segfaults on the deferred savedata flush — harness --savefile bus-copy is the working SRAM persistence path (recipe in capabilities.md; power-cycle proof in docs/proof/session-5-sram-*). Replay scripts shifted +4 frames for the title screen (bisected; headless-boot.yml updated — its first-ever dispatch also surfaced the PyPI wheel gap fixed in PR #17). Supply chain remains CLOSED to trust-on-first-use (PR #13 pins). Post-merge review rows queued: docs/review-queue.md #12, #13, #16.
