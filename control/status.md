# gba-homebrew (game-lab Track B) · status

updated: 2026-07-11T03:10:00Z
phase: session 8 slice 5 (same worker continuing) — **Lumen Drift v1.2 "GRAZE THE LIGHT" shipped**: the one fun-forward risk/reward addition for the coordinator's directive (deepen Lumen Drift while the owner concept pick is pending) — a near-miss bonus on the crystal spikes that turns the hazards into the light economy's risk premium. `dist/lumen-drift.gba` refreshed to v1.2.
health: green
kit: v1.8.0 · check: green (full --strict exit 0 at close) · engaged: yes
boot: synced to origin/main `9573d22` (post PR #32, review queue EMPTY); slice heartbeat commit `2acb98f` (card re-opened born-red + work claim) pushed before work per rule 7; toolchain verified live in this container (devkitARM r68, python 3.11.15 + mgba 0.10.2 binding OK; baseline build reproduced slice 4's v1.1 sha256 `d7e9e91a…` exactly before any change)
last-shipped: this slice's PR (branch `claude/session-8-slice-5`, opened READY at close; merge state lands in the git record) — **Lumen Drift v1.2: graze the light.** WHY: the moment-to-moment decision surface was thin — crystals were pure keep-away, optimal play held the tunnel center, and between the rare shards nothing the player DOES changes the light pressure. WHAT: shaving a crystal WITHOUT dying (a 4px graze shell just outside the kill box) pays back a third of a shard's light refund, once per crystal cell per run (16-slot ring — no hover farming), with an original synthesized whisper (`ld_graze`, deterministic generator, existing WAVs byte-identical), a flare, a GRAZE HUD line and a GRAZE game-over-card line. The safe middle is a slow fade to black; the bold line hugs the spikes — and v1.1's density tiers now double as a richer fuel field with depth. PROOF (all in THIS container on the v1.2 build): world gen/physics/kill rules UNTOUCHED (graze is read-only sensing) → `check-cave.py` scope unchanged, re-run green (rows 0..20405, 0 breaks/gates, tripwire 20/22/27/33); ALL FOUR replay tiers GREEN with ZERO expectation edits — canonical (7 text + 14 audio/watch asserts; route never grazes, loop-tick lag constant 19 → graze sensing costs no vblank), deep run (grazes 8 times, physics provably identical: DEPTH 58/61/95 telemetry exact, 0 deaths), SRAM two-boot, 300-frame boot; NEW dispatch-tier CI graze proof (inline 300-frame hand-tuned route): hook[8] 0→1 at f180 with deaths 0, light telemetry hook[9] >100→0 SAME frame (refund fired), mixer silent f170 → voiced f183 → silent f200 (whisper acoustically isolated — no thrust/shard on route), second graze f212, deliberate row-30 crystal death f218, card shows GRAZE 2; screenshots committed `docs/proof/session-8-graze-hud.png` / `-game-over.png`. First-draft HUD placement bug (GRAZE line at y=-42 corrupted the y=-40 banners) was CAUGHT BY the deep-run replay's banner asserts and fixed to y=-30 — the tiers earn their keep. Telemetry hooks grew 8→12 words (slots 0-7 stable; 8 = grazed cells, 9 = run_frames). Build: `lumen-drift.gba` **166,828 B** sha256 `e2af7daa332df3d89ff4adc35773325c35381c66b1e69a93adff0fdae4ce33b9` (skeleton unchanged `bec1feae…`). Dist: `dist/lumen-drift.gba` = this exact build, provenance row updated, `docs/PLAYING.md` explains grazing, current-state ledger entry added.
blockers: none
orders: acked=001,002 done=001 (this repo's half), 002 EXECUTED (slice 2) — trigger `trig_0137SkvhXEJvwepX8aVNkcSn` "gba-homebrew hourly wake (ORDER 002)", cron `0 * * * *`, fresh session per fire in env `env_014P62UXP7cuK1bPPWWzg521`; **first-fire confirmation STILL PENDING** (scheduler-reported first fire was `next_run_at 2026-07-11T02:07:07Z`; slices 4–5 ran in the coordinator-driven worker session 02:15–03:10 and cannot see into fired sessions — the woken session's own status/PR trail is the confirmation artifact; next wake must record it here)
review-queue: EMPTY (since slice 3).
polish-debt (honest read for the next-slice decision, ranked): 1) **route-recorder promotion** (`tools/route-recorder.py` + a `--keys-file` flag) — THRICE-justified now; slice 5 got away with a short hand-tuned route but any deeper feature proof will need the autopilot rebuilt a third time; 2) **graze visual tell** — the whisper + HUD line carry the mechanic, but a 1px glow-ring flash when the shell is touched would telegraph it without reading text (one-line change, provable with one `--require-distinct` shot pair in the existing graze CI step); 3) **game-feel micro-polish** — no pause button, no volume/mute, death→restart grace is 15 frames (feels right but unmeasured with real hands); 4) **graze tuning knob unvalidated by a human** — 400 frames/graze and the 6px shell are reasoned + machine-proven, but the owner's hands are the acceptance test (flagged in the PR body); 5) kit follow-ups carried (apply-docs, legacy root `claims/`, auto-merge enabler wiring, PR #5 gate-rule upstream).
kit-follow-ups carried (still unclaimed): `upgrade --apply-docs` (14 template-improved docs), migrate/retire legacy root `claims/` (only its README remains), auto-merge enabler wiring decision (`adopt --wire-enforcement`), upstream the PR #5 born-red-card gate rule to substrate-kit
⚑ needs-owner (all carried):
- **Concept pick** — pick 1 of the 3 committed Track B concepts (Lumen Drift-deepening / Clockwork Courier / Shoal; `docs/concepts/session-1-concepts.md`). The menu is WITH THE OWNER; slices 4–5 executed the coordinator's announced default (deepen Lumen Drift). An explicit pick still gates the remaining sessions.
- **⚑ owner-click: create the Lumen Drift GitHub Release** (worker seat gets 403 on tags/releases). v1.2 SUPERSEDES the v1.1 tag suggestion once this slice's PR merges — suggested tag now **`lumen-drift-v1.2`**. Exact clicks: repo → Releases → "Draft a new release" → "Choose a tag" → type `lumen-drift-v1.2` (create on publish, target `main` after this PR merges) → title `Lumen Drift v1.2` → attach `dist/lumen-drift.gba` from the merged tree (166,828 B; sha256 `e2af7daa332df3d89ff4adc35773325c35381c66b1e69a93adff0fdae4ce33b9` — paste it in the notes) → point the notes at `docs/PLAYING.md` and the v1.2 "graze the light" entry in `docs/current-state.md` → Publish.
- **⚑ owner-click: merged-branch cleanup** — worker seat gets 403 on branch-delete; merged `claude/*` branches (through PR #32 + this slice's) can be deleted in one sweep from the branches page.
notes: worker seat (no tag/release/branch-delete perms — queued above). Walls carried unchanged: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA python core.load_save() segfault (--savefile bus-copy is the working path, re-exercised green this slice); devkitPro official infra Cloudflare-403 (leseratte10 mirror + SHA-256 pins). dist/ convention: player-facing games ship committed in `dist/` with provenance (v1.2 row live); skeleton stays CI-artifact-only; CI sha256-logs every from-source build for cross-checking.
