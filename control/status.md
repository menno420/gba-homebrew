# gba-homebrew (game-lab Track B) · status

updated: 2026-07-10T04:42:51Z
phase: session 4 complete — Lumen Drift increment 3 (shards + cave sections) merged on green (PR #12, merge 0a2becb) → concept doc's committed scope FIRST-COMPLETE; both routed quick-wins shipped (PR #13, merge f78174c); lane idle pending owner concept pick / next order
health: green
kit: v1.6.0 · check: green (full --strict exit 0, session card complete) · engaged: yes (substrate-gate + "ROM builds" ran live on PRs #11–#13, all green; PR #13's cold run live-exercised the new toolchain checksum pins)
last-shipped: PR #12 — Lumen Drift increment 3: spark shards (edge-hugging collectibles, collect = light-decay rewind + SPARKS HUD and game-over line) + 3 cave sections (THE BLUFFS teal / THE GALLERY amber wide halls with pillars / THE DEEP violet narrow with denser crystals) with entry banners; generic gl_stage.h + gl_tile_grid visit_overlaps + harness --keys-pattern; proof docs/proof/session-4-{bluffs-shard-before,bluffs-shard-after,gallery-banner,deep-banner,deep-surge,game-over}.png — ONE deterministic 1780-frame run (--require-distinct PASS). PR #13 — quick-wins: --assert-text OCR-free HUD/card text assertions + lumen-drift-proof dispatch-tier CI job (per-PR <60s budget untouched); SHA-256 pins on all 9 unsigned mirror packages + crtls commit pin.
blockers: none
orders: acked=001 done=001 (this repo's half; joint done-when closes when Track A's half lands in pokemon-mod-lab) · no new orders at HEAD (inbox re-read 2026-07-10T04:42:51Z immediately before this write, HEAD f78174c) · session 4 ran the standing default: Lumen Drift increment 3 + quick-wins (coordinator-directed; claim released at close)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md — Lumen Drift now carries a FIRST-COMPLETE note (committed scope done in 3 sessions; the note lists what a post-pick polish pass would add: SRAM best persistence, audio, title screen, light-radius mechanic, tuning). HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions — with Lumen Drift scope-complete the pick now decides polish-vs-transfer, not sunk work. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. NOTE: increments 1–3 built as Lumen Drift under the standing default (coordinator directive); the generic half (games/common/ input/kinematics/collision/run-state/stage, harness scripting + text assertions, all-games CI) transfers if the owner picks differently. UNBLOCKS: locks the target for post-first-complete polish (Lumen Drift) or transfers the scaffolding to the picked concept and retires the generic-scaffolding hedge.
notes: Lane-written per control/README.md; inbox re-read at HEAD (unchanged since seed — ORDER 001 only) immediately before this write. Supply-chain caveat CLOSED to trust-on-first-use: all 9 leseratte10 mirror packages SHA-256-pinned in tools/setup-toolchain.sh (PR #13; pins live-verified by that PR's cold CI run, 5m07s; warm budget unaffected — 46s/48s on PRs #11/#12). ⚑ upstream to substrate-kit still carried: both substrate-gate fixes (PR #3 no-card sentinel, PR #5 added-card-advisory). Backlog conveyor: both routed idea files shipped and flipped (promoted/shipped, PR #13); new idea captured on the session-4 card (proof-PNG crop regression baselines, pairs with the open session-3 SRAM idea). Post-merge review rows queued: docs/review-queue.md #12, #13.
