# gba-homebrew (game-lab Track B) · status

updated: 2026-07-11T01:26:10Z
phase: session 8 — ORDER 000 (studio-coordinator directive, owner refs Q-0259/Q-0265/Q-0266): toolchain verified in-container + reproducible-build hardening (PR #29). Lumen Drift stays SCOPE-COMPLETE; no gameplay code touched.
health: green
kit: v1.8.0 · check: green (full --strict exit 0 at close) · engaged: yes
boot: synced to origin/main `0a7689f` (post PR #28 kit v1.8.0); heartbeat commit `929bec6` (born-red card + claim) pushed before work per rule 7
last-shipped: PR #29 — https://github.com/menno420/gba-homebrew/pull/29 — ORDER 000: (a) toolchain PROVEN in a fresh container — tools/setup-toolchain.sh ran clean, every mirror SHA-256 pin matched (only host gap: `zstd`, apt-installed); (b) lumen-drift.gba BUILT FROM SOURCE locally: 163,636 bytes, sha256 `0eb8750a9de8df0846db8598e5b1f13fcb010280f33d4e2c57257bf5318af2cf` (~18s cold); skeleton.gba 114,464 bytes; (c) headless mGBA boot smoke test PASS (mgba-sdl + mgba==0.10.2 on py3.11.15, 300 frames, title screen renders non-blank); (d) rom-builds.yml now logs sha256 of every ROM and uploads games/*/*.gba + rom-sha256.txt as workflow artifact `gba-roms-<sha>` (14d retention); (e) tools/build.sh one-command build (run end-to-end) + docs/BUILDING.md recipe. PR state at this write: READY, CI running, auto-merge to be armed in the checks-pending window (rule 2) with REST merge-on-green as fallback — final merge state lands in the git record, not re-editable here.
blockers: none — toolchain fully installable in-container, nothing missing (no OWNER-ACTION needed for ORDER 000)
orders: acked=001,002 done=001 (this repo's half) · **ORDER 002 (self-arm wake routine, P1) SEEN and UNEXECUTED** — session 8 was a directed worker slice scoped to ORDER 000 only; ORDER 002 is the lane's next claim (inbox re-read at HEAD `0a7689f` at open AND immediately before this write — no other new orders)
review-queue: rows open for PRs #3, #5, #6, #8, #9, #12, #13, #16, #17, #20, #23 + new #29 (artifact-publishing rail question, build.sh floor, BUILDING.md claims) — post-merge ledger per conventions rule 3
kit-follow-ups carried (from the v1.7.0→v1.8.0 wave cards, still unclaimed): `upgrade --apply-docs` (14 template-improved docs), migrate/retire legacy root `claims/` (only its README remains), decide on wiring the staged auto-merge enabler (`adopt --wire-enforcement`)
⚑ needs-owner: concept pick — WHAT: pick 1 of the 3 committed Track B concepts (Lumen Drift / Clockwork Courier / Shoal), jointly with Track A's 3 per ORDER 001. WHERE: docs/concepts/session-1-concepts.md — Lumen Drift is SCOPE-COMPLETE (committed scope + entire polish list built and proven; nothing further queued). HOW: any signal (inbox order or PR comment). WHY-IT-MATTERS: gates which game gets the remaining sessions — pure fork between new Lumen Drift scope (needs owner say-so) or transferring the scaffolding to Clockwork Courier / Shoal with zero sunk work. VERIFIED-NEEDED: yes — ORDER 001 reserves the pick to the owner. UNBLOCKS: the next build target for Track B's remaining sessions.
notes: session 8 was a studio worker seat (no tag/release/branch-delete perms — none needed this session; no ⚑ owner-click items queued). Walls carried unchanged: api.github.com curl proxy-walled (GitHub MCP is the merge path); mGBA python core.load_save() segfault (PLATFORM-LIMITS.md — --savefile bus-copy is the working path); devkitPro official package infra Cloudflare-403 (leseratte10 mirror route with SHA-256 pins is the build path, supply chain closed to trust-on-first-use since PR #13). ROMs remain uncommitted — CI builds from source and the new artifact step gives every commit a downloadable, sha256-logged ROM.
