# Releases — packaged artifacts, versions, hashes

> **Status:** `living-ledger`
>
> One row per packaged release artifact: what it is, where the file lives,
> its pinned sha256, and how to regenerate it byte-identically. Owner
> publish steps per channel: [`RELEASE-HOWTO.md`](RELEASE-HOWTO.md).
> Committed binaries/bundles in `dist/` are this repo's deliberate
> convention (`dist/README.md`).

## Web releases (cut 2026-07-13, session 40)

All four artifacts are generated by **`tools/package-web-arcade.sh`** from
**main @ `d87f9ad`**, cut 2026-07-13T03:41Z (`date -u`). The script is
deterministic (fixed source epoch `2026-07-13 00:00:00 UTC`, sorted zip
member list, `zip -X`): re-running it at the same tree byte-matches these
hashes — verified by two independent runs at cut time.

| Name | Version | File | Bytes | sha256 |
|---|---|---|---|---|
| Undertow (web) | v1.0 | `dist/releases/web-undertow-v1.0.zip` | 4,890 | `662f0e04a82dfa760f62bd2b47cd1054782a66d18467d0b23026b5978160dfcd` |
| Tiltstone (web) | v1.0 | `dist/releases/web-tiltstone-v1.0.zip` | 8,749 | `24c65a116e4a908fbb2046e22ea7a1c66e4098f142fb272f26eacb01b593b603` |
| Drift Garden PWA (foundation) | v0.1 | `dist/releases/drift-garden-pwa-v0.1.zip` | 6,241 | `d446f90a6bbc75b88f9d4cf8e83024e1fddb0b211d68627b3791fc96a03e2604` |
| Web arcade (all three + landing page) | v1.0 | `dist/releases/web-arcade-v1.0.zip` | 21,340 | `6f92498cc1c852bcee87a99bf096088322bf4f73f61fe40b983ed486660a8a33` |

Versioning: **v1.0** for the two finished web games (first cut; no prior
versions exist). **v0.1** for Drift Garden because what ships is the
**foundation PWA** on main — the Drift Garden game slice (PR #84) is
unmerged (see exclusions below). The unzipped, host-ready form of the same
content is committed at `dist/web/` (entry `dist/web/index.html`).

## ROM releases

| Name | Version | File | Bytes | sha256 |
|---|---|---|---|---|
| Lumen Drift (GBA) | v1.3 | `dist/lumen-drift.gba` | 167,996 | `195a86795e57e2fa0059a96782f1ac7a147cbcebc0cb28a96f353e5d9babae94` |

Lumen Drift v1.3 is release-ready and its **GitHub Release is still
pending the owner click** (worker seats get 403 on tags/releases) — the
queued click, with tag/title/attachment spelled out, is item 1 of
`docs/retro/archive-ready-2026-07-11.md`.

`dist/gloamline.nds` and `dist/brineward.nds` ship in `dist/` per-slice
(refreshed in the same PR as their game changes, provenance in
`dist/README.md`) but have **no release cut yet** — their arcs are still
mid-flight, so no versioned zip / Release row exists for them.

## Excluded because unmerged (as of this cut)

Packaged content derives from main @ `d87f9ad` **only**. Not packaged:

- **PR #82** — Brineward slices 6+7 (open).
- **PR #83** — Deepcast (open).
- **PR #84** — Drift Garden game slice (open) — hence the foundation-only
  v0.1 PWA above.

When these merge, re-run `tools/package-web-arcade.sh` (and bump versions
here) in the same PR that refreshes the artifacts.

## How to regenerate

```sh
tools/package-web-arcade.sh   # rebuilds dist/web/ + dist/releases/, prints sha256s
```

Compare the printed hashes against this table; any mismatch means the
committed artifacts and the source tree have drifted — fix in the same PR.
