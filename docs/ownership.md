# gba-homebrew — game-lab Track B: original GBA homebrew on Butano — ownership

> **Status:** `binding`
>
> Generated by substrate-kit. Which area / service / pipeline owns each
> table, event, and write path. **NOT SOURCE OF TRUTH** for code — source
> files always win.

> **Steady state:** this doc's table is **generated** from store / manifest
> specs where those exist — a projection, not hand-prose. This skeleton is
> the interim hand-maintained form until that projection lands.

## Ownership model

One agent lane (game-lab) owns all code and docs; the fleet manager owns control/inbox.md exclusively; owner (human) acts only on flagged click-level items under needs-owner

## Ownership table

| Area | Owner (module / service) | Writes it owns | Notes |
|---|---|---|---|
| (one row per owned area) | | | |

## New areas

New areas default to the lane; claim before build via one file per claim in claims/ (first-declared + claim-filed wins, fleet R10)
