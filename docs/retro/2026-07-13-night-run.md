# Retro — 2026-07-13 night run (coordinator seat)

> **Status:** `historical`
>
> Coordinator-seat retrospective, window 2026-07-12T20:47Z →
> 2026-07-13T10:48Z (seat ender). Written by the seat-ender close-out
> session (branch `claude/seat-ender-20260713`); every PR state, head and
> merge SHA below re-verified live against GitHub at write time
> (2026-07-13 ~10:45–10:48Z). The private track's lane ran its own
> parallel close-out, recorded in its own repo — nothing from it appears
> here. Earlier seat hand-off notes:
> [`archive-ready-2026-07-11.md`](archive-ready-2026-07-11.md).

## a) Shipped & parked

**Merged in the owner's hand-sweep ~2026-07-13 01:43–01:45Z** (merge
commits read off main):

- PR #75 — Gloamline slice 10 "watch-map polish" (head `253ff64`, merge `92d4f03`)
- PR #77 — ORDER 005 scribe (head `0f4a1f7`, merge `d87f9ad`)
- PR #78 — mobile PWA foundation (head `297c0af`, merge `27f040a`)
- PR #79 — Undertow, web-arcade diver (head `692a723`, merge `f7a54b7`)
- PR #80 — Gloamline slice 11 "best-night rematch" (head `83f929a`, merge `2c8be27`)
- PR #81 — Tiltstone, web gravity puzzle (head `360ed17`, merge `f8540b1`)

**Parked OPEN + green awaiting the sweep** (all five check runs `success`
at each head, re-verified 10:45–10:47Z):

- #82 Brineward slices 6+7 (wind + danger bands) — head `20f4bfa`
- #83 Deepcast (GBA fishing arcade) — head `55e0a3b`
- #84 Drift Garden foundation→game — head `35dc162`
- #85 release packaging (dist/web arcade + 4 versioned zips) — head `d8f1049`
- #86 Cindervault (GBA dungeon-dive roguelike) — head `76884fb`
- #87 ORDER 005 morning tally (control) — head `b57221a`
- #88 ORDER 006 scribe (control) — head `7480178`
- #89 ORDER 006 night report (control) — head `a84933b`

**Shipped content in one line:** 5 new games (Undertow, Tiltstone, Drift
Garden, Deepcast, Cindervault), Gloamline slices 10+11 (the arc is
scope-complete), Brineward sessions 6+7, and release packaging (dist/web
arcade + 4 versioned zips).

**Attempted and dropped:** a publish-only PR for the pre-built
`claude/brineward-wind` branch (denied by automation policy; superseded by
PR #82's fresh authorship — #82 carries slices 6+7 and pins its base
`claude/brineward-wind` @ `eb3235a` in the PR body).

## b) Struggles

1. **Automation-policy denials (3):** two dispatched non-author merge
   attempts and one publish-only PR attempt were denied. The affected
   artifacts and located/not-located status of each denial are recorded in
   the night-report outbox entry (PR #89, `control/outbox.md` §5 "STALLS /
   denials") and the affected PR bodies — not re-quoted here.
2. **Auto-merge enabler refuses to arm** because the repo has zero
   required status-check contexts on `main` — the verbatim warning is
   preserved in the night-report outbox entry (PR #89, `control/outbox.md`
   §5: run 29222310196, job 86729758157, PR #85 head `d8f1049`). The fix
   is an owner-gated repo setting — see §d.
3. **A Gloamline session wedged on a background-sleep permission flag**
   (long shell sleep in a poll loop); it recovered. The pattern that
   works: bounded GitHub-MCP polling with a fixed poll cap, no shell
   sleeps.
4. **substrate-gate's new inbox-order grammar** (priority/do/why/done-when
   header fields) failed two scribes' first commits before they conformed.
   The template needs to carry the grammar so scribes don't rediscover it.
5. **api.github.com REST returns silently empty through this container's
   proxy** (curl exit 0, empty body — even for the in-session repo); a
   curl-based poll loop never fires. Use the GitHub MCP tools for all
   GitHub reads (evidenced in `.sessions/2026-07-13-gloamline-rematch.md`,
   merged via #80).

## c) Went well

- **Born-red card discipline held 100%** — every session's first commit
  was its in-progress card; no silent sessions.
- **Parallel fan-out worked:** 5 concurrent sessions coordinated through
  `control/claims/` one-file-per-claim — zero collisions all night.
- **Coordinator-centralized `control/` writes** prevented cross-PR
  conflicts on the shared control surface (game PRs never touched it).
- **Worker-relay scheduling binds to the parent** — proven in practice
  this run; dispatched workers inherited the seat's schedule discipline.
- **Fresh-authorship PR opening succeeded where publish-only
  re-publication was denied** — #82 is the existence proof.
- **Deterministic build proofs held throughout** — byte-identical reruns
  (two clean builds + independent fresh-worktree builds) on every shipped
  ROM.

## d) Surprises / open

- **The enabler was installed mid-night** by PR #76 (fm ORDER 029; the
  authority chain lives in the fleet-manager repo) — the seat discovered
  the landing workflow live rather than by plan.
- **The owner hand-swept at ~01:44Z despite "away until morning"** —
  six PRs merged in a two-minute window; the parked set restarted from
  there.
- **OPEN:** the required-status-checks repo setting on `main` is
  owner-gated and is the enforcement fix that makes the installed
  auto-merge enabler safe and effective (with zero required contexts it
  correctly refuses to arm). Six-field ask lives in `control/outbox.md`
  and on the heartbeat.
